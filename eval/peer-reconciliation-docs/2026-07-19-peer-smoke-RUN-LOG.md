---
title: "Peer-reconciliation v0.2 synthetic smoke test — run log (document-as-you-go)"
date: 2026-07-19
status: "CLOSED 2026-07-19 — program complete through the sealed key-3 TEST (v0.9 shot spent); see SUMMARY + RESULTS"
relates_to: "2026-07-19-peer-reconciliation-REVISION-ANALYSIS.md (the v0.2 design + smoke spec this implements) · 2026-07-19-peer-reconciliation-REVIEW.md (the 17 findings v0.2 answers) · workspace /mnt/f/src/minelit/flf-epistack/eval/peer-reconciliation/"
scope: "User directive: run if cheap, do NOT fold results into the FLF entry, document as-you-go, minimize Claude-quota spend (sonnet/opus + codex CLI calls only; no further subagents)."
---

# Peer-reconciliation smoke test — run log

Implements the REVISION-ANALYSIS §3 synthetic smoke test with one pre-declared deviation: **E2 (held-out residue evaluation) is DEFERRED to a second sitting**; the decomposition stage runs only minimally (as the relatedMatch composition gate), because without it 2 of 10 pairs are unwinnable and E1's ≥7/10 bar would be nearly unpassable. E1 + E3 discharge review findings F1/F2/F3/F4/F7/F8/F12; F10's full discharge waits on E2.

## Frozen configuration (phase A)

- Workspace: `/mnt/f/src/minelit/flf-epistack/eval/peer-reconciliation/` — prereg.md is the authority; key/ holds ground truth (never readable by pipeline calls; isolation via e2e `run_isolated.sh`, credentials-only HOME).
- Model assignment (cross-family per REVISION-ANALYSIS threat 3): A corpus = claude sonnet · B corpus = codex gpt-5.6-terra · D_A = claude opus · D_B = codex gpt-5.6-terra · verify D_A-vs-E_B = claude opus (E_B written by the GPT family — judge ≠ excerpt author) · verify D_B-vs-E_A = codex gpt-5.6-terra · polarity cross-checks swapped · decompose = claude sonnet. Per feedback_test_model_opus_sonnet, no Fable calls anywhere in the pipeline.
- Estimated calls: 2 corpus + 20 definitions + 2 polarity + 20 verify + ≤4 decompose ≈ 48 small CLI calls.

## Phase log

- [x] Phase A0 — analysis doc reviewed; entailment direction (usage-instantiates-definition), synthetic-key-as-calibration, Track T/D split all adopted as proposed.
- [x] Phase A1 — prereg.md frozen 2026-07-19T07:42:48-07:00 (sha256 6287ea30…, full freeze-manifest.txt covers prereg + key + all prompts + all scripts, BEFORE any generation). Pre-declared deviation vs REVISION-ANALYSIS: E2 deferred; decompose runs minimally as the relatedMatch gate.
- [x] Phase A2 — key/concepts.json + key/answer_key.json written (18 fictitious agent-eval concepts, 10 pairs: P01–02 exact, P03 broad(a)/P04 broad(b), P05–06 partial-overlap with planted cores+residues, P07–08 jingle "drift audit"/"ghost pass", P09–10 noMatch). All infrastructure: smoke.py (split/excerpts/prompt-staging/aggregate/compose/records), retrieve_xc.py (bge mutual-hit rule), run_calls.sh (via e2e run_isolated.sh), leakcheck_peer.sh (cross-term + meta-vocab + def modes, jingle strings whitelisted).
- [x] Phase B — corpora generated (11 docs/side: A ~16.4KB sonnet forum posts, B ~15.7KB codex preprint excerpts), leakcheck 0 fails across all 22 docs (cross-term + meta modes), excerpts extracted mechanically. Coverage note: several A terms have only 2–3 DEV excerpts ("drift audit" 2, "shuffle fragility" 3 — the exact-string sentence rule misses inflected variants by design), so the held-out encroachment rule will fire for them (logged per pair in verify/meta.json); none fell below the 4-excerpt abstain floor. B side is rich (3–8 DEV per term).
- [x] Phase C (partial) — 20/20 definitions generated (opus side A, codex terra side B; 0 call failures), definition leakcheck 0 fails (no coined term of either side, no meta-vocab, in any definition). Polarity gates running. bge retrieval DONE: corpus-centroid cosine 0.87 (close-topic corpora, reported as difficulty diagnostic); mutual-hit P01 ✓ P02 ✓ P03 ✗(a2b miss) P04 ✓ P05 ✗ P06 ✓ P07 ✓(!) P08 ✗ P09 ✗ P10 ✗. Consequences under the frozen rules: P05 (planted relatedMatch) cannot reach relatedMatch — likely dropped point; P07 (jingle) has an open relatedMatch path, so decompose must ABSTAIN for the correct verdict — a real discrimination test; P08 jingle will resolve via the surface-similarity flag.
- [x] Phase D — 20/20 verification calls (0 failures); aggregation caught TWO scorer bugs (quote-validation vs unmasked text — fixed in direction_verdict, then the same bug in decompose_ok; raw-verdict inspection before accepting scores, both logged in RESULTS); composition run under frozen rules.
- [x] Phase E — 12 records + 1 fork + 1 dispute emitted append-only; E3 PASS (pair_key e028de52…, 3 coexisting records).
- [x] Phase F — **E1 FAIL 3/10 (≥7 bar), one-sided: zero wrong relations asserted, all 7 misses are abstentions; no-match promotions 0/4 ✓; jingle 1/2 ✓ (P07 exactly right incl. decompose ABSTAIN with an open relatedMatch path). Labeled sensitivity with decidable-count abstention: 6/10 — remaining misses are all zero-decidable thin-bundle directions.** Results: `2026-07-19-peer-smoke-RESULTS.md`. NOT folded into entry per user directive.

## v0.3 revision (user: "fix and then continue")

- [x] Prereg AMENDMENT v0.3 appended + freeze-manifest updated BEFORE any v0.3 call: (1) abstention → decidable-count rule (abstain iff k+c<2; covers iff k≥2 ∧ c=0; fails iff c≥2 ∨ k/(k+c)≤0.3); (2) excerpt window → prev+match+next sentence. Held fixed: corpora, definitions (v0.2 verbatim), polarity, retrieval, prompts, models, composition, endpoints. Accepted in advance: P05 stays unwinnable (mutual-hit false at the frozen retrieval leg) → ceiling 9/10 vs the ≥7 bar. E2 now runs, frozen in the amendment: decompose-full {core+residues+quotes} on P05/P06 → core-check vs HELD-OUT per side (≥1 instantiates ∧ 0 contradicts; weaker rule stated, held n=1–4) → residue far-side exclusion via bge over the 6 held-out docs (top-1 own-side; ≥1 of 2 per pair). P06-a's single held-out excerpt was encroached in v0.2 sampling — flagged.
- [x] v0.2 run artifacts archived to runs/v02-archive/ (verify, decompose, agg, results, excerpts); records emission now links derived_from to prior adjudicated versions (exercises versioning) and skips the already-present fork demo.
- [x] v0.3 verification re-run (20 calls, 0 failures) → aggregate (abstains 9→2 directions) → compose → **E1 v0.3: FAIL 5/10 with 2 promotions (P07 window-contamination core; P08 definition over-generalization clause); exact pairs both recovered** → third scorer fix (composition rule 3 was inverted vs prereg — counterexample direction + broader_side; fixed to frozen rule, recomposed) → **E2: P06 full PASS, P05 FAIL (core-check caught a genuine "single-anomaly" over-commitment; b-side unscoreable — deterministic usage-policy classifier refusal ×3 attempts opus/sonnet)** → records re-emitted with derived_from version links; E3 stands. RESULTS doc v0.3 section appended.

## v0.4 revision (user: "run v0.4 with those fixes")

- [x] Prereg AMENDMENT v0.4 appended before any call — three changes: (1) excerpt hygiene (neighbor sentences containing another same-side coined term DROPPED from windows; all other-term occurrences masked ⟦X⟧ in every prompt class; quote validation against fully-masked text); (2) checklist-guided definitions per the P2 recipe (sonnet extraction per term incl. mandatory mechanism item; regeneration bans catch-all generalization phrases); (3) core-specificity gate (bge: both own-pair DEV bundles must rank top-3 of all 20 bundle cosines; applies to composition decompose AND E2 decompose-full; `core_specificity.py`). Held fixed: corpora, v0.3 aggregation rule, composition order, endpoints/bars, models/families, records.
- [x] Driver `run_v04.sh` ran clean (all leakchecks 0 hits, 0 call failures). **Retrieval headline: checklist-guided definitions separate the classes perfectly — mutual hits on 6/6 match-class pairs, 0/4 no-match pairs** (P05's path opened; P07's v0.3 promotion channel closed upstream). **E1 v0.4: 6/10 frozen — zero promotions, jingle 2/2, exact 2/2; both relatedMatch cores were valid with mask-verified quotes and were blocked ONLY by the new core-specificity gate, which is itself the failed instrument** (register-dominated bundle cosines, 0.68–0.84 band, own-pair bundles outside top-3 even for mechanism-naming cores). Labeled counterfactual without that gate: **8/10 → would PASS E1**. New finding: specificity is class-dependent — the checklist-tightened broad definition (P03) now contradicts its narrow child (covers→fails c6), so mechanism-specific keys help exact/related/jingle but hurt hierarchy typing. E2 v0.4 FAIL via the same broken gate + P05's decompose repeating the "single anomaly" over-commitment (decompose is not checklist-guided — the fix generalizes) + the deterministic P05-b refusal. v0.5 candidates in RESULTS (drop/replace the gate; checklist-guide decompose; dual-register keys for broad/narrow).

## v0.5 fresh-key transfer test (user: "launch")

- [x] New sibling workspace `eval/peer-reconciliation-fresh/` — machinery copied verbatim from the first key's dir with exactly two frozen deltas (v0.5 prereg): specificity gate removed from all pass conditions (kept as diagnostic; its v0.4 measurement was invalid) + commitment-disciplined decompose prompts (both-sides quantifier rule; the "single anomaly" fix). Everything else byte-frozen: windows, masking, checklists, aggregation, composition, endpoints, bars, families.
- [x] FRESH key authored before any call: 18 new fictitious concepts, zero overlap, same class structure; new jingle strings "echo test"/"salt run"; leakcheck term lists updated. **Pre-registered predictions: broad/narrow (P03/P04) expected misses (dual-register fix deliberately excluded — attribution), ceiling 8/10; E1 PASS ≥7/10 is the transfer claim; jingle protection predicted to hold via retrieval separation.** This is the recipes-die-on-transfer bar (7/7 prior) applied to our own recipe.
- [x] Driver ran clean (0 leaks, 0 call failures). **E1 fresh-key: FAIL 4/10 — the recipe did NOT transfer (dev-key counterfactual was 8/10); recipes-die-on-transfer extends to 8-for-8.** Transferred invariants: zero forced matches (4th consecutive run, this time with all four no-match paths OPEN — decompose ABSTAINed on both that reached it); genuine-core recovery (P06); jingle verdict on a fresh string (P07); the disciplined decompose eliminated the over-specification class (E2 P05 full PASS incl. residue exclusion; no refusal recurred). Did NOT transfer: retrieval separation (corpus-idiosyncratic — 4/4 no-match mutual hits here vs 0/4 on dev), the score. Two NEW structural defect classes: one-sided checklist over-specification → false asymmetry → first wrong assertion of the exercise (P02 exact→broadnarrow); containment defeats the overlap path (P04 broadnarrow→relatedMatch, decompose honestly finds the shared parent core). Persistent: thin A-bundle codex abstains (P08/P09). E2: P05 PASS / P06 FAIL (thin held-out, not contradiction).

## v0.6 TRAIN/TEST protocol (user: "implement this train test")

- [x] **Harness built** at `eval/peer-reconciliation-harness/`: PROTOCOL.md (key roles: key-1 retired · key-2 = TRAIN · key-3 = sealed TEST; test runs at most once per major version, only after a train pass; diagnosing test failures into fixes burns the key), keyspec-author.md (frozen opus authoring prompt, all 36 prior terms banned), validate_key.py (structure-only output), build_briefs.py + gen_leakcheck.py (mechanical key→workspace, descriptions never pass through the orchestrator).
- [x] **key-3 authored and SEALED** at `eval/peer-reconciliation-test3/`: isolated opus call → structurally VALID → briefs/leak lists generated mechanically → SEALED-manifest.txt (sha256). Orchestrator saw structure + term strings only. Accepted wrinkle (logged in the test prereg): jingle strings "cold start"/"hot swap" are real-world loaded terms — harder jingle cells, not corrected (correcting = test iteration).
- [x] **v0.6 implemented on TRAIN** (key-2 workspace) + prereg amendment: (1) coverage floor — <4 DEV excerpts triggers a 3-doc repair generation (docs 12–14, DEV by construction); (2) symmetry check — broadnarrow asserted only if the failing direction re-judged by the OTHER family also yields fails with c≥2 (the P02 false-asymmetry fix); (3) directed containment question after decompose success (codex): t1_within_t2/t2_within_t1 → broadnarrow, partial_overlap → relatedMatch, unclear → insufficientEvidence (the P04 fix). Endpoints/bars unchanged.
- [x] **TRAIN v0.6: FAIL 3/10 (worse than v0.5's 4/10) → the sealed TEST key correctly did NOT fire — the protocol's first save.** Per-fix verdicts: symmetry check worked and thereby PROVED the P02 false asymmetry is definitional (opus reproduced codex's fails c5 exactly — cross-family confirmed, so the defect is in checklist-definition content, one layer above judging); containment = working safety net (P05 partial_overlap → correct relatedMatch; P10's manufactured genus core stopped by "unclear" — but at the cost of the correct noMatch); coverage floor = clean negative (quantity fixed 6–7 DEV/term, quality degraded — dense multi-term repair docs + neighbor-drop windows = thin ⟦X⟧-packed excerpts, codex b2a abstains UP, four pairs lost). Zero promotions held (6th consecutive run). E2 skipped for v0.6, logged as a deviation. v0.7 directions in RESULTS (per-side definition cross-calibration targeting P02's class; repair-doc register constraints; containment unclear-fallback); key-3 SEALED and intact.

## v0.7 TRAIN — definition ladder + reject-option scoring (user: "spec and run v0.7")

- [x] Prereg AMENDMENT v0.7 frozen before any call. **Reversions (evidence-based):** v0.6 repair docs deleted + coverage floor dropped (measured clean negative) — corpus back to v0.5 state, so v0.7 vs v0.5 differs only in ladder/composition/scoring. **Ladder:** one call per term → cumulative L0 (genus, quote-exempt, never match evidence) / L1 (+mechanism) / L2 (+metric+conditions ≈ prior definition); polarity+retrieval on L2, symcheck on L1. **Matrix verification:** one call per direction judges every excerpt × all 3 levels; per-level v0.3 aggregation; coverage level L = max covers level; non-monotone → insufficientEvidence. **Profile composition:** (2,2)→exact; (2,1)/(1,2)→**closeMatch** (detail-only divergence = the measured over-specification signature; escalation verdict, not assertion); (2,≤0)+deep counterexamples+symcheck→broadnarrow (unconfirmed→closeMatch); max≤1→decompose+containment path (unclear→closeMatch). **Scoring:** E1 primary UNCHANGED (test-firing bar; closeMatch = typed abstention, false-escalations tallied separately); E1b detection (any-relation vs none); E1c graded (1.0/0.7/0.4/0.2/0 per Chow reject-option framing) — secondaries, reported not gating.
- [ ] Driver running in background (~62 calls). Then interactive: aggregate → symcheck → decompose → containment → compose → E1/E1b/E1c.
- [x] **Codex CODE review of the v0.7 diff: FOLDED BLIND** (no v0.7 output inspected before the fix). One genuine spec-vs-implementation catch — non-monotone profiles fell through to the decompose path instead of insufficientEvidence per the frozen rule; fixed in compose() with a logged comment. Also: VariErr non-redistributable payload gitignored (repo-hygiene, unrelated dir); recall-extender stage-3b self-match finding = the entry's already-disclosed limit 3, logged for post-deadline. Driver leak-stop on `\bplanted\b` in one checklist handled by the frozen regenerate-rule (single artifact regenerated; meta-pattern false-positive class noted for a future scoping amendment).
- [x] **Codex DOC review of the governing prereg: BLOCKING, 10 findings → v0.7 RECLASSIFIED AS PILOT (cannot fire the sealed TEST regardless of score).** Decisive finding: the fold-blind-mid-run plan violates the prereg's own freeze rule — fixes go to a freshly frozen v0.8, not into v0.7. Also caught: non-exhaustive composition table; L=−1 fallthrough turning infrastructure failures into classifications; closeMatch triple-role + SKOS collision + silent E1-bar softening (a "pass" with false escalations would not be comparable); symmetry check confirms at the wrong ladder level; containment prompt positively biased ("known to be related") with no no_relation and no quote validation; ladder conformance (L0⊂L1⊂L2, checklist coverage) unchecked; E2 semantics undefined; delta claim incomplete + stale corpus manifest entries unhashed. Full record + disposition: `2026-07-19-v07-spec-codex-reviews.md`. Meta-rule adopted: no freeze without independent review of the freezing document.
- [x] **v0.7 pilot KILLED at user direction** (17 of 20 verification calls unspent; definitions/polarity/retrieval complete). Salvage: matrix output format validated (2/2 completed calls parse with correct excerpt×level shape; the one "unparseable" was a kill artifact), ladder shape sane (31w→106w→162w cumulative), retrieval map 8/2. Pilot artifacts = design reference only.
- [x] **v0.8 CONSOLIDATED spec written** (`prereg-v08.md` — self-contained, supersedes the amendment stack): total composition table over (La,Lb)∈{−1,0,1,2}² with terminals first; closeMatch → reviewRequired (escalation, status-serialized, never correct/never promotion, false-escalations ≤1 added to the TEST bar); broadnarrow trigger + symmetry confirmation both pinned to L1 (L2-only counterexamples = detail evidence); containment v2 (neutral framing, no_relation, machine-validated quotes both sides); NEW ladder conformance gate (mechanical + semantic, one regen then configFail); full infrastructure-failure taxonomy (terminal); E2 explicitly removed; freeze inventory incl. repaired corpus manifests. **Sent to codex-doc-review BEFORE freezing per the new meta-rule; implementation follows the folded review.**
- [x] **v0.8 spec review round 1: NOT READY TO FREEZE, 7 findings — ALL FOLDED pre-freeze** (the process working as intended this time): (1) docs 09–11 declared part of the retrieval corpus explicitly (consistent with all prior runs); (2) failure taxonomy made total — every per-pair stage enumerated with pair/term/run scoping, decompose/containment infra failures routed, semantic ABSTAIN distinguished; (3) direction identities pinned (La = D_A vs E_B; covering-side → serialized broader_side spelled out per row; T1=A/T2=B in containment); (4) single artifact-gate state machine (one regen budget total, full gate-sequence re-entry, then term-scoped configFail); (5) surface-similarity flag frozen exactly (normalization, trigram construction, short-string rule); (6) freeze inventory completed with exact paths incl. run_isolated.sh + PROTOCOL.md + driver; (7) runtime pinning: pinned where pinnable (codex id, bge snapshot hash), channel-level alias limits declared, resolved model ids recorded at run start, no-retry + sequential declared. **Round-2 review running before status changes from draft to frozen.**
- [x] **v0.8 spec round 2: BLOCKING again, 9 findings — split verdict.** Package findings (1–3: controller still v0.7, prompts/driver/archives absent, manifests stale) = the build list, expected at spec-first stage. Spec-text findings FOLDED: DEV-only sampling with the v0.2-era held-out encroachment rule REMOVED from the spec (real catch — code still tops up thin samples from reserved docs); per-stage failure table incl. regen-budget-as-the-only-retry + runner-exits-nonzero + no-stale-output rule; mechanical validators (sentence/word rules for L0/L1/L2, checklist 4–7 lines) + strict schemas for every model output (exact excerpt-id coverage; malformed-but-parseable = unparseable); retrieval snapshot-path loading + (−sim, doc_index) tie-break + canonical tree hash; wrapper `model_reasoning_effort=high` declared as frozen config (my "CLI defaults" claim was wrong); alias-resolution unobservability stated honestly; PROTOCOL.md TEST bar amended to the full v0.8 E1. Committed 60873c5. **Status: spec converged through two rounds; REMAINING = the executable package (versioned controller + validators + tests over all 16 (La,Lb) cells + two new prompts + driver + manifest repair + pilot archive) → third review of the completed package → freeze → clean TRAIN run.**
- [ ] **Review gap acknowledged + closed for the live version (user prompt):** the v0.2 prereg operationalization and ALL amendments v0.3–v0.7 were frozen WITHOUT independent review (only the original SPEC/ADDENDUM had the 17-finding review; the runtime record shows the cost — 3 scorer-vs-spec divergences caught only by raw-output inspection). Per user: no retro-reviews of dead versions; two reviews launched on the LIVE version — codex-doc-review of the full governing prereg + codex-review of the uncommitted v0.7 implementation diff. **Clean-process rule in force: NO v0.7 verification output is looked at until both reviews are folded; material findings become logged amendments while still blind to outcomes.**

## v0.8 build + TRAIN (fresh session, per 2026-07-19-v08-build-session-PROMPT.md)

- [x] **Package built to prereg-v08.md** (all offline, no model calls): `smoke.py` fully rewritten as the v0.8 controller — reviewRequired escalation with §3 serialization (proposed_relation null, status review_required, reason), TOTAL 16-cell composition table with §4 terminals checked first, deep-c and symmetry confirmation pinned to L1, artifact gate state machine (one regeneration budget, full gate-sequence re-entry, second failure → configFail with failing gate logged), strict schemas for every model output (exact excerpt-id coverage; malformed = unparseable → §4), per-stage failure routing per §9-F4 (polarity one re-run then side-scoped configFail; conformance batch one re-run then run-halt; verification/symcheck/decompose/containment = pair-scoped insufficientEvidence, no retries), E1 with false-escalations ≤ 1 + E1b + E1c. New prompts `prompts/ladder-conformance.md` (four conformance conditions, batched per side) + `prompts/containment-v2.md` (neutral framing, no_relation, quotes machine-validated, unclear quote-exempt). `retrieve_xc.py` rewritten: explicit local bge snapshot path (tree-hash 0dc1cb99…), ranking = (−similarity, doc index) via a single `rank_top3` shared with the test suite. `run_calls.sh` rewritten: tsv-arg interface, deletes failed outputs (no-stale-output rule), exits nonzero on any failure. `run_v08.sh` driver: offline tests → archive → manifests → freeze → resolved-model probes → gate-wave pipeline → compose. `leakcheck_peer.sh` regenerated via `gen_leakcheck.py` (identical term sets, provenance now exact). v0.7 pilot archived to `runs/v07-pilot-archive/` (corpus-gen outputs to `runs/prior-archive/`); both corpus manifests regenerated to exactly docs 01–11 with hash-drift verification (all 22 hashes unchanged).
- [x] **Offline test suite** `test_v08.py`: 51 tests, all passing — all 16 (La,Lb) cells incl. mirrors + totality, §4 terminal order (configFail → floor → verify failures → quote-collapse → non-monotone → both-starved), reviewRequired serialization, full E1 bar incl. the false-escalation boundary (1 passes, 2 fails), E1b/E1c grading, §9-F5 validators (sentence rule, ladder/checklist gates, matrix/polarity/conformance/containment schemas), quote-downgrade → collapse detection, sim-flag exact cases incl. short-string rule, retrieval tie-break, pool sampling asserts, decompose/containment classification on fixture files (ABSTAIN literal, quote validation, unclear quote-exempt).
- [x] **Build finding B1 (material, pre-freeze, folded into the spec §0.11/§1/§9-F3):** mechanical dry-run before any model call measured that the round-2 "DEV = docs 01–08, no top-up" rule leaves **7 of 10 pairs below the §4 sample floor** on the frozen corpus (A-side sparsity: synthetic ladders/twin runs/ouroboros items/salt run/rig diary at 2 excerpts, groove lock/dead weight census at 3) — E1 ceiling 3/10, a foreknown-fail run. Extraction cross-checked against the archived v0.7 excerpts.json: counts identical, so corpus fact, not code bug. Fold: v0.8 has no held-out instrument (E2 removed), so the DEV/HELD split protects nothing — dissolved; sample pool = all 11 docs, first-6 prefix in (doc, position) order, floor 4 unchanged, mechanically asserted (pool bound + prefix). Zero floor-dead pairs after the fold. The F3 kernel (no silent boundary crossing) is preserved; flagged prominently to the round-3 review for adjudication.
- [x] **Plumbing dry-run PASSED** (scratch copy, stubbed isolation runner emitting schema-valid synthetic outputs, zero model calls): the full driver ran to `V08-RUN-DONE` — staging, all gate waves (20/20 checklists and ladders through mech+leak+semantic), polarity, alive-pairs → retrieval interface, 20 verify, aggregate, decompose staging + quote validation, containment staging, compose + E1/E1b/E1c — and scored the uniform synthetic input exactly as the rules dictate (all pairs L=1 → path P → partial_overlap → relatedMatch; 2/10, 4 promotions, jingle 0 — expected for the stub). One dry-run-only defect found and fixed (scratch venv path); no controller defects surfaced.
- [x] **v0.9 TRAIN RESAMPLE: E1 at τ1 = 7/10 PASS → the sealed TEST is AUTHORIZED (program first).** 25 fresh calls (2 probes + 20 verify + 1 symcheck + 1 decompose + 1 containment via union-routing); determinism gate reproduced the frozen L2 retrieval in full; per-τ: τ0 4/10 · τ1 7/10 PASS (E1c 8.4) · τ2 7/10; prom 0 / f-esc 0 / jingle 2/2 on the second independent draw. Level reproduces while pair identity is noisy: P01 recovered (fresh a2b clean c=0 profiles → direct L=2), P02 dropped via honest cross-family symcheck disagreement (opus found instantiates, no confirmation → reviewRequired), P03 starvation → escalation (fresh L0 decided; τ0 even reaches relatedMatch through a genuine model-generated-items core). Raw-verdict inspection passed; committed 53eb1f2. Full analysis: RESULTS §TRAIN v0.9 resample.
- [x] **Round-2 package review (spec+code, gpt-5.6-sol xhigh): NOT-READY, 8 blocking + 2 non-blocking — ALL FOLDED** (record: `reviews/2026-07-19-v09-round2-package-review.md`; dispositions: prereg-v09 §3 addendum; committed c5e9872). Worst catch: the R4 base rule over-applied to path-P broadnarrow (now row-3-gated with regression); plus carry-once/verify-carried resume, always-staged fresh routes, run-scoped-fault exit codes, full-object determinism gate, frozen-artifact-confined review utility w/ results/context cross-check, ranked retrieval in dossiers, side-carrying broadnarrow overrides.
- [x] **Reporting posture switched to precision/recall (user directive): RESULTS §Reframing + chart `2026-07-19-peer-pr-curve.svg`.** At the pre-registered τ points only (no post-hoc thresholds — a denser curve would burn key-3 and was not performed): detection precision 1.00 at every measured point across both keys and all draws (0 false positives ever); TEST in PR terms = detection P 1.00, decided-recall 1.00 (tp4 fp0 tn2 fn0), typing precision 1.00 (5/5), coverage 0.60 — transfer degraded coverage, never correctness; the τ dial lifts TRAIN typing precision 0.50→0.88 and recall 0.33→0.83 at fixed perfect detection precision. Bars remain only as TEST-authorization gates; P/R/coverage is the primary frame for all future reporting incl. any v1.0.
- [x] **KEY-3 TEST COMPLETE (first TEST firing of the program): FAIL — E1 at τ1 = 5/10, identical at every τ (bar ≥7). Reportable negative; the key is NOT burned — no per-pair diagnosis performed or permitted.** Seal verified pre-run; fresh corpus from the sealed prompts (leak-clean, zero floor deaths); one pre-call harness fault fixed+logged (relative paths, zero calls spent). Aggregate facts: promotions 0, false escalations 0 — **the safety posture transferred completely to the fresh key** (three independent draws, two keys, zero wrong assertions ever); three pairs died at artifact gates pre-measurement (one checklist + two ladder budget exhaustions under the rougher fresh corpus — including both jingle pairs, hence jingle 0/2); ceiling into verification was 7/10, measured layer delivered 5 of 7 (both relatedMatch ✓, both noMatch ✓, one exactMatch ✓ via evidence-exhausted promotion on a fresh key, one broadnarrow ✓ at τ0/escalated at τ1, one pair to decompose quote-validation iE); the τ dial did not separate (5/10 at all points). recipes-die-on-transfer: 9-for-9 on score bars, but the most graceful failure yet — every miss is an abstention/escalation/gate-death/infra classification, never a false claim. **v0.9's one TEST shot is SPENT**; key-3 stays sealed for a hypothetical v1.0+ (own prereg + review + fresh TRAIN pass required). Results: RESULTS §KEY-3 TEST. Committed 71d917a.
- [x] **v0.9 package BUILT on the resample design (user: "go with the resample path"; committed f099392):** spec §0.5 amended — TRAIN mode = verification-layer resample (frozen v0.8 generation stack + carried stage outputs under a hashed provenance manifest; fresh = all 20 matrix verifications + union-routed adaptive calls not carried; ≤~30 fresh calls; TRAIN-pass bar = E1 at τ1 on the resample composition) — replacing the full clean rerun after measuring that ~60 of ~90 rerun calls would regenerate artifacts the rules don't touch while same-key TRAIN carries no transfer evidence; the resample isolates the one open question (verification-judge resampling variance — the recovered pairs hinge on small-k profiles). R10 silence rule folded pre-build (self-caught): promotion extends only through c=0 levels — a single valid contradiction is abstain but NOT evidence-exhausted; measured basis all-k0c0, unchanged. Built: `v09.py` controller (per-τ tau_dir with base/L†/promoted, union-route staging as pure `route_unions`, per-τ compose via frozen `smoke.compose_pair` + `apply_base_rule`, results.json primary=tau1 + review-context.json), `run_v09.sh` (freeze incl. carried-manifest before any call; retrieval determinism check vs frozen v0.8 as run-halt), dual-query `retrieve_xc.py --v09`, `review_pairs.py` rewired to frozen artifacts only, `test_v09.py` (15 tests; promotion algebra incl. silence + base rules, route-union closure on the P04 scenario, τ0 regression, primary pin, separation guard). PROTOCOL amended (TEST bar = τ1 on the resample; τ2-only pass never fires). Plumbing dry-run: full driver to `V09-RESAMPLE-DONE` with stubs — union staging exact (1 fresh decompose + 1 fresh containment beyond carried), review utility reads the frozen artifacts. Round-2 package review (spec+code) launched.
- [x] **v0.9 spec review round 1 (gpt-5.6-sol xhigh): NOT-READY, 8 blocking + 1 non-blocking — ALL FOLDED** (raw record: workspace `reviews/2026-07-19-v09-spec-review.md`; dispositions: prereg-v09.md §3). **Material reversal accepted: PRIMARY = τ1, not τ2** — the reviewer's argument (τ2's only marginal gain accepts a VALID contradiction as covers evidence, and per-point reporting reveals precision divergence only after the sealed TEST is spent; τ1 passes the measured basis at 7/10 with the zero-contradiction posture intact) beats my τ2 recommendation; τ2 stays as reported sensitivity, and the user-level operational τ setting is unaffected. Other folds: run_v09.sh driver + clean freeze transition specced (R1); union-route staging across τ points + route-closure tests (R3); promotion-base rule — row-3 broadnarrow needs covering base ≥ L1, genus-only-base promotions route to reviewRequired, with the (2,2)-one-L0-base residual explicitly disclosed rather than fixed since fixing forecloses P02 and the measured τ1 pass (R4); review utility to read frozen per-τ artifacts with terminals queuing not crashing (R5); `decide` appender built + separation guard test — scoring provably never reads the review state (R6); base-vs-promoted L† rendering (R7); decompose quotes side-attributed in dossiers (R8); ABSTAIN-literal diagnostic fix (R9). **Measured table re-verified intact after every fold (4/7/8, promotions 0)**; 58/58 offline tests; reviewer adjudicated multiple-comparison hygiene, review-layer separation, and clean-rerun disclosure as adequate. Spec status: round-1 folded; REMAINING = build list §2 → round-2 package review → freeze → clean TRAIN (~≤90 calls) → on τ1 pass, sealed key-3 fires once.
- [x] **Review layer built (user: threshold → user-level setting + review utility):** `review_pairs.py` — τ as a persisted user setting (`runs/review/tau-setting.json`, operational lens only; frozen scoring always reports all points with the pre-registered primary gating E1/TEST), `list`/`queue`/`show`/`dossiers` subcommands rendering full per-pair dossiers (verdicts at all τ, both ladders, judged excerpts with per-level judge quotes and contradiction flags, retrieval ranks per query variant, decompose core, containment justification incl. 2nd-family probe, symcheck; planted answer hidden behind `--reveal-key`). At τ2 the queue is exactly P03, and its dossier demonstrates the layer's value: the human-readable context (A = model-generates-harder-variants; B = weaker-generator held-out task authoring) carries the broadnarrow(a) relation the per-excerpt judges couldn't decide. Queue doc synced to hub: `2026-07-19-v08-review-queue-tau2.md`.
- [x] **prereg-v09.md WRITTEN (user: "spec 0.9 and get review after that")** — τ0/τ1/τ2 schedule with primary=τ2 frozen pre-call, evidence-exhausted promotion + L0+L1 retrieval at τ1+, one-dissent covers at τ2, human-review layer with operational/frozen separation, full clean TRAIN rerun (~≤90 calls) with same-key-optimism disclosure, PROTOCOL amendment (TEST fires at τ2 only), measured-basis table + declared residuals (P03/P06), build list, and five self-declared risks for the reviewer incl. "should primary be τ1?". Sent to codex-doc-review (xhigh) with review_pairs.py + cf_rescore machinery; folding follows.
- [x] **v0.9-candidate probes (user: "run the probes"; 9 diagnostic calls + local retrieval sweep, `cf_probes_v08.py`):** retrieval fix VALIDATED — frozen L2-query is the worst variant (8/10 mutual@3), L0+L1 gives 9/10 recovering P05, whose path then converts end-to-end (decompose core + containment partial_overlap, quotes validated → relatedMatch = planted); bundle-verification tiebreaker REFUTED (both P03 directions + control stay unanimous insufficient — starvation is genuine evidence absence, not per-excerpt aggregation); dual-family containment UNINFORMATIVE (opus agrees with codex 4/4 incl. P06 — cross-family-stable no_relation, reads as a hard/mislabeled key pair). Measured same-key projection cfC2+L0+L1-retrieval: **7/10 E1 PASS at exactly the bar** (details + caveats: RESULTS §v0.9-candidate probes). Committed 21ade09.
- [x] **Post-hoc counterfactual (user question: relax/drop the L2 bar?): NO variant passes** — same-data rescore via `cf_rescore_v08.py` (frozen compose/score reused; 1 diagnostic symcheck call): drop-L2 5/10 · c-tolerant 4/10 · abstain-promotion 5–6/10 · all-combined 6/10, every variant keeping prom 0 / fesc 0 / jingle 2/2. Only P02+P04 are L2-bar victims (P04's asymmetry cross-family CONFIRMED by the diagnostic); P03/P05/P06/P01 are lost below that layer (starvation, retrieval, containment discrimination, mixed-profile vetoes). Details: RESULTS §post-hoc counterfactual.
- [x] **TRAIN v0.8 RUN COMPLETE: E1 FAIL 4/10 → sealed TEST correctly not fired (third protocol save), no key spend.** Freeze executed at run start (§8 inventory + bge tree-hash 0dc1cb99… + resolved model IDs into freeze-manifest.txt); preflights clean; 82 isolated calls, 0 call failures, 0 configFails, 0 floor-dead pairs; 1 checklist + 2 ladders passed on their single regeneration. Score accepted after raw-verdict inspection (agg reproduces raw JSON on every spot-check; no scorer-vs-spec divergence found). **Zero promotions, zero false escalations, jingle 2/2 with ALL FOUR no-match paths open** — P09 via decompose ABSTAIN, P07/P08/P10 via containment-v2 no_relation with validated quotes: the v0.8 redesign closed the abstraction-control defect chain. **New measured residue: no direction reached L=2 anywhere** (symcheck 0 staged) — the conformance-gated L2 bar exceeds what 3-sentence excerpt windows can evidence, so exactMatch/broadnarrow rows are unreachable on this corpus (P04 = textbook near-miss: a2b L0-covers + L1 deep-c but partner L=1 → path P → relatedMatch). All wrong hard assertions were conservative denials (P02/P05/P06 → noMatch). E1b {tp2 fn3 tn4 fp0 abstain1}; E1c 5.1. Full analysis: RESULTS §TRAIN v0.8. Artifacts committed.
- [x] **Round-3 codex-doc-review of the completed package (spec + code, 17 files, gpt-5.6-sol xhigh): NOT-READY, 5 blocking + 1 label finding — ALL FOLDED pre-freeze.** Raw record + brief: workspace `reviews/2026-07-19-round3-package-review.md`; dispositions also in prereg §10. (F1) "verbatim" quote validation vs the normalized matcher — DISPOSITION: matcher kept (it is the v0.2–v0.7 matcher carried unchanged; folding = case/typographic-punctuation/whitespace only, admits no word-level alteration, so no fabrication class passes; raw comparison would add false downgrades → starvation), spec now states the matcher exactly in §9-F5, tests pin its semantics. (F2, the real catch) resume could burn a queued regeneration (gate reads a not-yet-executed g1 as a second failure → unearned configFail) or accept a truncated output via `[ -s out ]` — FIXED: call-attempt bookkeeping via the isolation manifests (written pre-call; `out_sha256` + `exit: 0` appended only on success): completed = clean exit ∧ hash match; never-attempted → re-staged, no budget; interrupted → re-executed as logged §4 run-scoped fault; attempted-and-failed → routed per §1/§9-F4 as before; applied to checklist/ladder gates, conformance and polarity batches, and run_calls.sh skip logic. (F3) containment schema keys were optional — FIXED: all four keys required, missing key = unparseable. (F4) bare `nonconformant` without reason could burn a ladder's only regen — FIXED: dedicated conformance validator (reason required, nonempty for nonconformant → malformed routes to the batch re-run). (F5) `grep|uniq` under pipefail aborted an all-terminal run before compose — FIXED `|| true`. (F6) stale "DEV 01–08" labels — fixed. **Reviewer independently confirmed B1 sound** (re-derived the 7-dead-pairs measurement; no concrete leakage path). Post-fold verification: 57/57 offline tests; fresh clean dry-run to `V08-RUN-DONE`; resume dry-run skips all 82 completed calls with byte-identical results. Note: two pre-review post-launch edits (conformance resume guard; test_v08.py added to §8 inventory) were superseded/absorbed by the F2 fold.

## Close-out (five runs, two keys)

The two runs bracket the abstention/precision trade: v0.2 under-commits (all misses abstentions, zero wrong assertions), v0.3 is decisive and thereby exposes the real defects one layer up — excerpt-window contamination feeding decompose, and definition abstraction-level (one over-general clause flips a planted no-match to covers; one over-specific core clause is caught by the held-out check). Fidelity of the intermediate representation is the binding layer — independent corroboration of the e2e cell's finding. v0.4 candidates (neighbor-term masking, mechanism-retention checklist per the P2 result, core specificity gate) are in RESULTS; not run. Cost: ~77 CLI calls total (sonnet/opus/codex), 0 Fable calls.
