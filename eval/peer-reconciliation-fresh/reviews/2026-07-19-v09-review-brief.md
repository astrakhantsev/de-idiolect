You are reviewing the peer-reconciliation v0.9 pre-registration BEFORE its freeze, plus the two supporting code artifacts it promotes into the package. Context: v0.8 ran TRAIN and failed E1 4/10 with all safety invariants intact (0 promotions, 0 false escalations, jingle 2/2); post-hoc counterfactuals and probes (committed) measured a τ dial ladder 4/10 → 7/10 → 8/10 on the same artifacts with promotions 0 at every point. v0.9 pre-registers that dial, a human-review layer, and a clean TRAIN rerun. prereg-v08.md governs everything v0.9 does not explicitly change; you have it for cross-reference.

FILES: prereg-v09.md (the spec under review), prereg-v08.md (governing base, already 3-rounds-reviewed — do NOT re-review it), review_pairs.py (the human-review utility being promoted into the package), cf_rescore_v08.py (the measured-basis machinery the spec cites), smoke.py (current v0.8 controller the build list will modify — for cross-reference of the τ implementation surface).

REVIEW SCOPE:
1. Spec §0 exactness — are the τ definitions implementable without interpretation? Check: the promotion algorithm (base required, consecutive pure-abstain extension, mixed/fails stop), τ2 covers/fails disjointness claim, retrieval query rules per τ (one execution, two queries recorded), and whether composition-on-L† vs raw-count deep-c/symcheck/monotone is stated unambiguously.
2. The five risks the spec itself lists in §3 — adjudicate each: is the mitigation adequate, or does the spec need a change (e.g. primary τ1 instead of τ2)? Give a definite recommendation per risk.
3. Multiple-comparison hygiene: could any reading of the spec let a τ1-pass/τ2-fail TRAIN fire the TEST, or let the reported non-primary points influence any gate?
4. The human-review layer separation: any path by which the user-level τ setting or decisions.jsonl can contaminate frozen scoring or the TEST protocol? Is excluding human decisions from E1 stated airtight?
5. review_pairs.py — does the dossier renderer faithfully represent the underlying data (direction labels a2b/b2a and their judge/ladder/excerpt sides, per-level counts, quote attribution, promotion vs raw levels)? A rendering swap here misleads human adjudications — check the direction semantics against smoke.py's identities (a2b = D_A vs E_B judged by opus; b2a = D_B vs E_A judged by codex). Also: the key-hiding default (--reveal-key), and whether the queue definition (non-asserted at active τ) matches the spec's §0.4.
6. cf_rescore_v08.py assemble()/variant_dir() — do they implement the spec §0.2 τ definitions exactly (esp. promotion and the c1 covers rule)? Any divergence between what was MEASURED (the §1 table) and what the spec now FREEZES would silently invalidate the measured basis.
7. Anything in the build list (§2) that under-specifies the smoke.py/retrieve_xc.py changes enough that the implementation could diverge from the measured counterfactuals.

OUT OF SCOPE: prereg-v08.md content (frozen, reviewed), the corpus/key design, model/family choices, the TRAIN/TEST protocol's existence, style. This is an exploratory-programme instrument: flag only defects that would change a verdict, mislead a human adjudication, waste the call budget, or weaken the pre-registration's bindingness.

OUTPUT FORMAT: numbered findings, most severe first, each: [BLOCKING or NON-BLOCKING] — defect — exact file/section/line — concrete consequence — minimal fix. Then a one-line adjudication per §3 risk (1–5). If a dimension checks clean, one line "checked, nothing found". End with FREEZE-READY or NOT-READY + blocking count. Verify findings against the actual text (quote the decisive line); no speculative or stylistic findings.
