---
provenance: codex-doc-review round 3 (completed package, spec + code), gpt-5.6-sol effort=xhigh, 2026-07-19; brief archived alongside; raw output verbatim below. Dispositions: hub 2026-07-19-peer-smoke-RUN-LOG.md v0.8 section + prereg-v08.md §10.
---

1. **[BLOCKING] — Quote validation is normalized rather than verbatim** — [smoke.py:567](/mnt/f/src/minelit/flf-epistack/eval/peer-reconciliation-fresh/smoke.py:567), `level_verdict`, `decompose_result`, `containment_result`; spec §§2.7–2.10. Decisive line: `q ... = norm(...)` followed by `q not in norm(masked_texts[i-1])`. Lowercasing, punctuation folding, and whitespace collapsing allow altered quotes to count as evidence; a probe with lowercase quotes against capitalized excerpts produced `k=2, covers` with no downgrades. This can raise La/Lb, confirm symmetry, or admit decompose/containment instead of insufficientEvidence. Minimal fix: compare the raw quote, at most stripping outer whitespace, against the exact masked excerpt string; add case/punctuation/whitespace-mutation tests.

2. **[BLOCKING] — The advertised resumability can lose a queued retry or accept a partial output** — [run_v08.sh:2](/mnt/f/src/minelit/flf-epistack/eval/peer-reconciliation-fresh/run_v08.sh:2), [run_calls.sh:17](/mnt/f/src/minelit/flf-epistack/eval/peer-reconciliation-fresh/run_calls.sh:17), `prompts_checklist`, `prompts_def`, `prompts_conformance`, `prompts_polarity`. Decisive lines: checklist/polarity staging always targets `g0`/`r0`, while gates select `g = regens_used` or `r = reruns_used`; `run_calls.sh` skips any nonempty output solely because `[ -s "$out" ]`. If interrupted after a gate records `pending_regen`/`reruns_used=1` but before `g1`/`r1` completes, restart can immediately produce configFail or conformance run-halt without executing the authorized retry; a truncated nonempty checklist can also be skipped and pass. Minimal fix: make staging derive the pending generation/attempt from persisted state, process outstanding queues before re-gating, and accept an existing output only when its manifest records successful completion and a matching hash—prefer atomic temp-output rename.

3. **[BLOCKING] — Containment’s required schema fields are treated as optional** — [smoke.py:134](/mnt/f/src/minelit/flf-epistack/eval/peer-reconciliation-fresh/smoke.py:134), `validate_containment`; prereg §9-F5 and [containment-v2.md:15](/mnt/f/src/minelit/flf-epistack/eval/peer-reconciliation-fresh/prompts/containment-v2.md:15). Decisive line: `obj.get(k, "")` accepts an absent `quote_1`, `quote_2`, or `justification`. Thus `{"relation":"unclear"}` becomes reviewRequired rather than insufficientEvidence, while a decided relation with valid quotes but no justification can become a hard relation. Minimal fix: require all four keys with string values before relation-specific quote validation; test every missing-field case.

4. **[BLOCKING] — A malformed nonconformance row without its required reason is accepted as a semantic ladder failure** — [smoke.py:117](/mnt/f/src/minelit/flf-epistack/eval/peer-reconciliation-fresh/smoke.py:117), `validate_flat_rows` and [smoke.py:428](/mnt/f/src/minelit/flf-epistack/eval/peer-reconciliation-fresh/smoke.py:428), `gate_conformance`; prereg §2.4 and [ladder-conformance.md:10](/mnt/f/src/minelit/flf-epistack/eval/peer-reconciliation-fresh/prompts/ladder-conformance.md:10). Decisive line: `r.get("reason", "")` makes the field optional. `[{"item":1,"verdict":"nonconformant"}]` therefore consumes the artifact’s sole ladder regeneration instead of taking the conformance-batch rerun route. Minimal fix: use a dedicated conformance validator requiring `reason=""` for conformant and a nonempty `condition 1–4: ...` one-sentence reason for nonconformant.

5. **[BLOCKING] — An all-terminal run aborts before composition** — [run_v08.sh:134](/mnt/f/src/minelit/flf-epistack/eval/peer-reconciliation-fresh/run_v08.sh:134), retrieval-summary pipeline. Decisive line: `grep -o '"mutual": [a-z]*' ... | sort | uniq -c` under `set -o pipefail`. If a polarity batch fails twice, every pair is correctly side-scoped configFail, `alive-pairs.json` is empty, retrieval succeeds with no `mutual` records, and `grep` exits 1; the driver stops before writing `results.json`. Minimal fix: make this diagnostic succeed on zero matches, e.g. append `|| true` to the pipeline or summarize with Python.

6. **[NON-BLOCKING] — The B1 execution log still falsely labels sampling as DEV 01–08** — [run_v08.sh:81](/mnt/f/src/minelit/flf-epistack/eval/peer-reconciliation-fresh/run_v08.sh:81) and [test_v08.py:6](/mnt/f/src/minelit/flf-epistack/eval/peer-reconciliation-fresh/test_v08.py:6). Decisive line: `excerpts (DEV = docs 01-08 only...)`. Runtime extraction correctly uses all 11 documents, but the frozen log would contradict the amendment. Minimal fix: change both labels to “all 11 docs; no reserved split.”

Composition/terminal ordering, all 16 cells, L1-pinned deep-c and symmetry, path-P routing, serialization, E1/E1b/E1c, retrieval directionality/determinism, and uninterrupted gate waves: checked, nothing else found.

Freeze inventory/order and the two new prompts: checked, nothing found.

B1 adjudication: sound. I mechanically confirmed the stated five A terms with 2 excerpts, two with 3, seven dead pairs under 01–08, and zero under 01–11. With E2 removed and no pipeline access to the answer key, this creates no concrete new held-out leakage path.

Offline suite: 51/51 pass, but it does not exercise the concrete cases in findings 1–5.

**NOT-READY — 5 blocking findings.**