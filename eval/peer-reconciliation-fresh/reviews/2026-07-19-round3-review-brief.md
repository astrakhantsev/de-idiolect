You are performing the THIRD review round of the peer-reconciliation v0.8 package — this time the COMPLETED EXECUTABLE PACKAGE (spec + code together), before the freeze. Rounds 1 and 2 reviewed the spec text alone; all their findings are already folded into prereg-v08.md. The package will be frozen verbatim after this review, then a TRAIN run executes it.

The files follow, each delimited and labeled. prereg-v08.md is the sole authority; smoke.py is the controller; run_v08.sh is the driver; run_calls.sh executes staged calls via the e2e isolation runner (run_isolated.sh); retrieve_xc.py is deterministic local retrieval; test_v08.py is the offline suite (51 tests, all passing); ladder-conformance.md and containment-v2.md are the two NEW prompts; the other prompts are carried frozen from v0.7 and are included for cross-reference.

REVIEW SCOPE (stay inside it):
1. Spec-vs-implementation divergence — does smoke.py implement prereg-v08.md EXACTLY? Composition table §5 (total over 16 cells, terminals-first §4, L1-pinned deep-c and symmetry, path-P routing), artifact gate state machine §1 (one regeneration budget total, full gate-sequence re-entry, second failure = configFail), per-stage failure routing §9-F4 (generation failures consume the regen budget; polarity one re-run then side-scoped configFail; conformance batch one re-run then run-halt; verification/symcheck/decompose/containment failures = pair-scoped insufficientEvidence, no retries), mechanical validators §9-F5 (exact word/sentence rules, strict schemas, quote validation and downgrade), endpoints §6 (E1 with false-escalations ≤ 1, E1b, E1c), serialization §3 (reviewRequired as status, proposed_relation null).
2. Driver orchestration — does run_v08.sh order phases per §2, freeze the full §8 inventory BEFORE any pipeline model call, keep the wave logic consistent with the state machine (a regenerated ladder re-enters gates from the start incl. a second conformance batch), and never run downstream stages over unresolved upstream failures?
3. NEW prompts — do ladder-conformance.md and containment-v2.md operationalize §2.4b and §2.10 faithfully (four conformance conditions; neutral containment framing, no_relation, quotes required for decided options, unclear quote-exempt)?
4. **Adjudicate build finding B1** (spec §0.11, §1, §9-F3): at build time, before any model call, the round-2 "sampling from docs 01–08 only, no top-up" rule was measured to leave 7 of 10 pairs below the §4 sample floor on the frozen corpus (A-side sparsity: five terms with 2 excerpts, two with 3), i.e. an E1 ceiling of 3/10 — a foreknown-fail run. Since v0.8 removed E2, no held-out instrument remains, so the amendment dissolves the DEV/HELD split: the sample pool is all 11 docs, first-6 prefix in (doc, position) order, floor 4 unchanged, mechanically asserted. Question: is this amendment sound and honestly stated, or does it reintroduce a leakage/contamination path you can name concretely? (Retrieval already used all 11 docs in every prior run; generation bundles and verification samples are same-side usage excerpts; the answer key is never readable by any pipeline call.)
5. Offline test suite — any UNTESTED spec-critical behavior that could plausibly diverge at run time (name the concrete case; do not ask for coverage ceremony).

OUT OF SCOPE (do not relitigate): the key/corpus design (frozen, key-2 TRAIN), findings already folded in rounds 1–2 (reviewRequired rename, table totality, containment v2 design, conformance gate existence, E2 removal, runtime pinning limits), the choice of models/families, the TRAIN/TEST protocol itself, and style/naming/formatting preferences. This is an exploratory-programme instrument, not production software — flag only defects that would change a verdict, waste the ~70-call budget, or silently misroute a failure.

OUTPUT FORMAT: a numbered list of findings, most severe first. For each: [BLOCKING or NON-BLOCKING] — one-line defect statement — the exact file/section/function — the concrete failure it causes at run time — the minimal fix. If you verify a dimension and find nothing, say "checked, nothing found" for that dimension in one line. End with a one-line verdict: FREEZE-READY or NOT-READY plus the blocking count. Verify each finding against the actual text before reporting it (quote the decisive line); do not report speculative or stylistic findings.
