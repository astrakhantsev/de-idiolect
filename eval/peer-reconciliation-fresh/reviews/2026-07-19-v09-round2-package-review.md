---
provenance: codex-doc-review round 2 of the v0.9 package (spec + code; gpt-5.6-sol xhigh, 2026-07-19); dispositions: prereg-v09.md §3 addendum.
---

1. [BLOCKING] — the promotion-base rule is applied to every `broadnarrow`, not only row 3 — [v09.py:259](/mnt/f/src/minelit/flf-epistack/eval/peer-reconciliation-fresh/v09.py:259), `compose_tau`/`apply_base_rule`, lines 259–269 — decisive line: `if v.get("proposed_relation") == "broadnarrow"`; authority says “a row-3 broadnarrow assertion” only — reproduced case: path-P `(base_L,L†)=(0,1)/(0,0)` plus containment `t2_within_t1` correctly yields asserted `broadnarrow(a)` from `compose_pair`, then becomes `reviewRequired(promotion-base-genus-only)` — pass the composition route/row-3 candidate into `apply_base_rule` and gate R4 on row 3; add this exact regression.

2. [BLOCKING] — a resume can mutate the carried inputs after freeze — [run_v09.sh:18](/mnt/f/src/minelit/flf-epistack/eval/peer-reconciliation-fresh/run_v09.sh:18), phase 1/2, and [v09.py:104](/mnt/f/src/minelit/flf-epistack/eval/peer-reconciliation-fresh/v09.py:104), `carry_stages` — decisive lines: `python3 v09.py carry-stages` executes before `if [ ! -f runs/v09/.frozen ]`, while `dst.write_text(...)` and `json.dump(...carried-manifest.json...)` overwrite the frozen files — a changed v0.8 source can therefore replace both carried outputs and their manifest while the old freeze hash remains authoritative — run copying only inside the first-freeze branch; on resume, verify the existing manifest and every destination hash without writing.

3. [BLOCKING] — adaptive resumability bypasses `run_calls.sh` completion validation — [v09.py:187](/mnt/f/src/minelit/flf-epistack/eval/peer-reconciliation-fresh/v09.py:187), `stage_adaptive_1`, and [v09.py:224](/mnt/f/src/minelit/flf-epistack/eval/peer-reconciliation-fresh/v09.py:224), `stage_adaptive_2` — decisive line: `if out.exists(): continue  # carried`; an interrupted fresh call can leave an output after its manifest was created but before `out_sha256`/`exit: 0`, whereupon restaging emits no TSV row and the runner never performs its intended `RE-EXEC` path — distinguish manifest-listed carried destinations from fresh outputs, and always restage fresh routes so `run_calls.sh` decides completed/failed/interrupted.

4. [BLOCKING] — the driver converts run-scoped harness faults into pair verdicts — [run_v09.sh:10](/mnt/f/src/minelit/flf-epistack/eval/peer-reconciliation-fresh/run_v09.sh:10), `calls`, and [run_calls.sh:41](/mnt/f/src/minelit/flf-epistack/eval/peer-reconciliation-fresh/run_calls.sh:41) — decisive line: `bash run_calls.sh "$1" || echo "NOTE: failures ... routed per §4"` swallows every nonzero status, while the authority says “harness/scorer crashes” are run-scoped faults — a missing/broken isolation runner is treated as missing verification/adaptive output and can become `insufficientEvidence` instead of halting for repair — assign distinct exit statuses to ordinary model-call failures versus runner/system faults and absorb only the former.

5. [BLOCKING] — the retrieval determinism gate compares only `mutual`, not the frozen L2 results — [retrieve_xc.py:70](/mnt/f/src/minelit/flf-epistack/eval/peer-reconciliation-fresh/retrieve_xc.py:70), `main` — decisive condition: `out[pid]["L2"]["mutual"] != frozen[pid]["mutual"]`; the governing requirement is that “the L2-query results must reproduce v0.8’s frozen runs/retrieval.json” — changed directional hits, rank order, document IDs, or similarities can pass if the final Boolean is unchanged, defeating the run-halt gate and changing evidence shown to reviewers — compare the complete per-pair L2 objects, including both directional `hit`/`top3` records, against the frozen file.

6. [BLOCKING] — the review utility is not confined to the declared frozen v0.9 result set — [review_pairs.py:60](/mnt/f/src/minelit/flf-epistack/eval/peer-reconciliation-fresh/review_pairs.py:60), `build_all`, loads only `review-context.json`, while [review_pairs.py:174](/mnt/f/src/minelit/flf-epistack/eval/peer-reconciliation-fresh/review_pairs.py:174), `dossier`, directly reads `runs/cf-diagnostic/out-cont2-*.json`, files absent from the v0.9 freeze and carried manifest — decisive lines: `cf_ = V09 / "review-context.json"` and `probe = RUNS / f"cf-diagnostic/out-cont2-{pid}.json"` — an altered diagnostic file can change a supposedly frozen dossier, and a results/context mismatch is never detected — load and cross-check `results.json` plus `review-context.json`, and copy/hash review-only probes into a frozen v0.9 manifest or serialize them into review context.

7. [BLOCKING] — dossiers omit the required retrieval ranks — [review_pairs.py:157](/mnt/f/src/minelit/flf-epistack/eval/peer-reconciliation-fresh/review_pairs.py:157), `dossier` — decisive rendering is only `a2b=hit|miss/b2a=hit|miss (mutual=...)`, although §0.4 requires “retrieval ranks per query variant” — a reviewer receives neither ranked documents nor similarities despite the “full dossier” claim — render each query’s ordered `top3` entries for both directions.

8. [BLOCKING] — a human `broadnarrow` override cannot record the broader side — [review_pairs.py:211](/mnt/f/src/minelit/flf-epistack/eval/peer-reconciliation-fresh/review_pairs.py:211), `cmd_decide` — decisive validation accepts bare `broadnarrow` through `HARD_OR_NONE`, while a side-qualified form such as `broadnarrow(a)` is rejected — the appended disposition is ambiguous and cannot represent the relation vocabulary’s required `broader_side` — accept an explicit side-qualified grammar and persist `relation` plus `broader_side` structurally.

9. [NON-BLOCKING] — the advertised route/schema/gating coverage is absent and failed to catch finding 1 — [test_v09.py:88](/mnt/f/src/minelit/flf-epistack/eval/peer-reconciliation-fresh/test_v09.py:88), `TestBaseRule`, injects a bare broadnarrow without establishing its route, and [test_v09.py:135](/mnt/f/src/minelit/flf-epistack/eval/peer-reconciliation-fresh/test_v09.py:135), `test_primary_is_tau1`, checks only the constant — concrete untested cases are path-P containment producing broadnarrow from a promoted L0 base, interrupted fresh adaptive output, full route-output closure, results schema, and final gating when τ2 passes but τ1 fails — add focused tests for those cases.

10. [NON-BLOCKING] — command help still identifies the retired counterfactual mode and τ2 as the recommended primary — [review_pairs.py:2](/mnt/f/src/minelit/flf-epistack/eval/peer-reconciliation-fresh/review_pairs.py:2), module help — decisive text: `runs today against the v0.8 TRAIN artifacts via the cf_rescore assembly` and `τ2 ... <- recommended primary` — invoking the utility without arguments gives the adjudicator guidance contradicting the frozen τ1 decision and the later data-source paragraph — update the help to the v0.9 resample and τ1 primary.

Checked clean — §0.2 remainder: τ-specific covers boundaries, consecutive-silent promotion, no-base/τ0 behavior, monotone/starved recomputation, raw-L1 deep-c/symcheck mechanics, τ-specific mutual mapping, and the tolerated exactMatch residual match the authority.

Checked clean — §0.5 carried membership: the 16 unique mappings exactly cover the specified seven v0.8 decompositions, six containments, P05 diagnostic pair, and P04-a2b symcheck; source output hashes match their completed invocation manifests.

Checked clean — §2.3 normal-path closure: union construction uses the fresh aggregation across all τ, includes each τ’s retrieval rule, stages containment after any union-routed successful decomposition, and does not require adaptive output for pairs absent from aggregation.

Checked clean — driver normal phase order: tests, carry, freeze, model provenance, deterministic retrieval, fresh verification, aggregation/adaptive stages, then composition; ordinary failed stage calls are deleted and route without retry.

Checked clean — results/scoring remainder: all three score objects contain per-pair verdicts, `primary` is `tau1`, the final authorization reads `results[PRIMARY]`, and review state is absent from scoring.

Checked clean — review remainder: queue predicates include all three non-asserted statuses, terminals do not crash, base versus promoted levels and side-attributed decompose quotes are distinct, decisions append UTC records, and dispositions do not contaminate scoring.

Checked clean — executable checks: `test_v09.py` passed 15/15 and shell/Python syntax checks passed; the read-only review sandbox prevented eight tempfile-dependent v0.8 tests from starting, while its other 50 tests passed.

NOT-READY — 8 blocking findings.