You are performing the ROUND-2 review of the peer-reconciliation v0.9 package — the COMPLETED EXECUTABLE (spec + code together) before its freeze and the ~26-call TRAIN resample run. Round 1 reviewed the spec alone; its 8 blocking findings are folded (prereg-v09.md §3 records the dispositions, including primary τ2→τ1, the R4 promotion-base rule, the R10 silence rule, union-route staging, and the resample TRAIN mode that replaced the full clean rerun). Do NOT re-litigate round-1 dispositions or prereg-v08.md (three rounds, frozen, ran).

FILES: prereg-v09.md (authority), v09.py (the resample controller), run_v09.sh (driver), retrieve_xc.py (dual-query + determinism check), review_pairs.py (frozen-artifact review utility), test_v09.py (15 tests, passing), smoke.py (frozen v0.8 primitives that v09.py imports — verify the import surface only, do not re-review it), test_v08.py (regression suite, passing), prereg-v08.md + PROTOCOL.md (cross-reference).

REVIEW SCOPE:
1. v09.py vs prereg-v09 §0.2 EXACTNESS: tau_covers per point; tau_dir promotion (base required; extension only through SILENT levels k+c<2 ∧ c=0; mixed/fails/c≥1 stop; τ0 never promotes); monotone/starved recomputation; deep-c/symcheck on RAW L1 counts; apply_base_rule scope (row-3 broadnarrow only, exactMatch residual deliberately tolerated); per-τ mutual mapping (τ0→L2, τ1/τ2→L0L1).
2. §0.5 resample semantics: carried_map completeness and correctness (v0.8 decompose P01/P04/P06–P10 + containment P01/P04/P06/P07/P08/P10 + diagnostic P05 dec/cont + P04-a2b symcheck — is anything carried that shouldn't be, or missing such that union-routing would stage a redundant or absent call?); the carried-manifest hashing; v0.8 tree never written.
3. Union-route staging closure (§2.3): route_unions + stage_adaptive-1/-2 — can any per-τ composition require a stage output that is neither carried nor staged? Pay attention to: containment required at a τ where decompose succeeded but the pair is path-P only at ANOTHER τ; symcheck candidates that appear only after fresh aggregation; pairs absent from agg.
4. run_v09.sh: phase order, freeze-before-calls (incl. carried-manifest inside the freeze), determinism-check gating, resumability via run_calls.sh completion semantics, failure routing at verification/adaptive stages (§4 no-retry).
5. compose/results/review-context emission: per-τ verdicts via smoke.compose_pair with the τ-adjusted dirs — any way the τ-adjusted dirs violate compose_pair's assumptions (e.g. pretable checks reading recomputed monotone/starved vs raw collapsed)? results.json schema with primary=tau1; E1 gating on the primary only.
6. review_pairs.py rewiring: reads only frozen v0.9 artifacts; direction/level rendering (base_L vs promoted L†); queue predicate; decide appender; no scoring contamination.
7. test_v09.py: any spec-critical v0.9 behavior that could plausibly diverge at run time and is untested (name the concrete case).

OUT OF SCOPE: round-1 dispositions, v0.8 content, corpus/key design, model choices, style. Flag only defects that would change a verdict, waste calls, break the freeze discipline, or mislead a human adjudication.

OUTPUT FORMAT: numbered findings, most severe first: [BLOCKING or NON-BLOCKING] — defect — exact file/function/line — concrete runtime consequence — minimal fix. One line per checked-clean dimension. End with FREEZE-READY or NOT-READY + blocking count. Verify each finding against the actual text (quote the decisive line); no speculative or stylistic findings.
