You are writing short excerpts from method-and-results sections of preprints by a research group that studies the evaluation of LLM agents. This group has its own technical vocabulary, listed below. Write EXACTLY 11 excerpts.

THE GROUP'S TERMS (its own coinages — use them exactly as given, no synonyms, no abbreviations, no other names for these ideas):

1. "salt run" — A run in which a handful of items with known correct answers are hidden among the real items, to spot-check mid-stream whether the judge is still scoring accurately; judge misses on the known items trigger a halt.
2. "item discrimination exhaustion rate" — Measuring an evaluation suite's health by the fraction of its items that no longer discriminate between agents or versions — items everything passes or everything fails; high rate = the suite has stopped carrying information.
3. "strategy cardinality index" — A count of how many structurally distinct tool-use strategies appear across an agent's successful runs on a task set — higher counts read as behavioral diversity rather than a single memorized approach.
4. "matched contrast probes" — Constructing matched task pairs that differ only in how much irrelevant distractor context is included, and measuring how the agent's tool usage shifts between the two — the shift quantifies distraction sensitivity.
5. "tier floor annotation" — Labeling each evaluation item with the minimum capability tier expected to solve it, so that failures can be triaged as expected or surprising given the agent's tier.
6. "subordinate authoring protocol" — The specific protocol where evaluation tasks are generated by a model strictly weaker than the agent being evaluated, so tasks remain independently verifiable while still challenging the stronger system.
7. "reflexive adjudication" — Having the agent under evaluation grade anonymized outputs that include its own, and measuring whether its agreement with other graders drops on its own outputs — a drop indicates self-recognition contaminating the grades.
8. "trajectory template carryover" — An agent that recently succeeded with a particular sequence of actions repeats that same sequence on a new task where it does not apply — inappropriate replay of a recently successful action pattern; the errors disappear when session history is cleared.
9. "echo test" — Checking whether an agent copies phrasing from the few-shot examples into its final answer where that phrasing is inappropriate — verbatim example carryover into free-form output.
10. "resource perturbation study" — Any intervention that changes an agent's resource budget mid-task — tokens, wall-clock time, or tool-call quota, raised or lowered — to observe how the agent adapts its strategy to the changed budget.

RULES:
- Register: impersonal preprint prose — setup, procedure, quantitative results, limitations. Invented datasets, model names, and numbers are fine. Each excerpt 150–300 words with a short section-style title.
- NEVER define the terms. No "X is defined as…", "X refers to…", "we call this X", no glossaries, no introduction-of-terminology sentences. Use each term as established in-group vocabulary while reporting concrete procedures and findings. A newcomer should have to infer meanings from context.
- Each of the 10 terms must be used in AT LEAST 4 different excerpts, with at least 2 sentences naturally using the term in each of those excerpts. Each excerpt should feature 2–4 of the terms. Each of excerpts 9, 10, 11 must use at least 5 of the terms.
- Use each term ONLY for its listed meaning. Do not coin any additional vocabulary for these ideas.
- Do not mention other communities, vocabularies, or anything about naming.
- Output format, exactly: each excerpt starts with a line `<<<DOC NN>>>` (NN = 01..11), then `# Title`, then the body. No other framing text before, between, or after.
