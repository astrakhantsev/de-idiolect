Three DEFINITIONS of the same concept at increasing specificity:

DEFINITION L0 (genus only): A procedure for creating held-out task sets used to evaluate agents.

DEFINITION L1 (adds mechanism): A procedure for creating held-out task sets used to evaluate agents. Weaker task generators, rather than the evaluated agents, produce a fixed number of task specifications or scenarios, each with an executable check or validator withheld from the evaluated agents. Generators cannot observe evaluated-agent traces; the procedure is rerun for each model family or repeated with newly sampled generators and fresh validators, preventing one task-writing distribution from dominating. Tasks are filtered for execution validity and duplicate surface form, human review rejects ambiguous state transitions, and comparisons normalize available-tool counts and instruction length.

DEFINITION L2 (adds measurement and conditions): This is a procedure for generating task or benchmark sets for evaluating agents, including data-management scenarios. In each run, weaker held-out task generators—not the evaluated agents—produce a fixed number of task specifications, such as 2,160. Every task includes an executable check or validator, but evaluated agents do not receive it. The generators are prevented from seeing evaluated-agent traces. The procedure is rerun for each model family, or later rounds use newly sampled generators and fresh validators, so one task-writing distribution does not dominate comparisons. Generated tasks are filtered for execution validity and duplicate surface form. Human review also rejects tasks with ambiguous state transitions; in one set it rejected 11%, leaving 1,740 tasks. Results are read after normalizing for the number of available tools and the length of task instructions.

Below are numbered excerpts from a community's documents in which one term — masked as ⟦TERM⟧ — is used (other local jargon masked ⟦X⟧).

For EACH excerpt, and for EACH definition level INDEPENDENTLY, decide: is the referent of ⟦TERM⟧, as used in THIS excerpt, an instance of the concept described by THAT definition?

- "instantiates" — the usage is consistent with and exemplifies that definition.
- "contradicts" — the usage is incompatible with that definition (the referent has a property that definition excludes, or lacks one it requires).
- "insufficient" — this excerpt alone does not contain enough to decide for that definition.

A usage can instantiate L0 while contradicting L2 — judge each level on its own content. Judge each excerpt on its own text only. For L1 and L2, any decided verdict (instantiates or contradicts) requires a verbatim supporting quote from the excerpt; L0 needs no quote.

Output ONLY a JSON array, one object per excerpt:
[{"excerpt": 1, "L0": "instantiates|contradicts|insufficient", "L1": {"verdict": "...", "quote": "verbatim or empty for insufficient"}, "L2": {"verdict": "...", "quote": "..."}}, ...]

EXCERPTS:

1. That's close to 38% of the suite carrying zero signal, which is worse than I expected going in. Also want to flag: a chunk of the items that are still "live" in the census were generated through ⟦TERM⟧ last spring, and I don't think anyone re-validated them against actual production incidents since. Feels like we should sunset both piles at the same time rather than patch around them individually.
2. Got into it with a teammate over the new "adversarial" tier we added to the coding benchmark. Turns out all 60 items came from ⟦TERM⟧ — a model was given the old benchmark's easy tier and asked to generate harder variants, no human wrote a single one from scratch. He doesn't think that's a problem as long as pass rates look reasonable.
3. Cleared history, it diagnosed the real cause immediately, so same story as always: recency, not reasoning. Meanwhile I've been fighting with our ⟦TERM⟧ tier again. Half the "advanced" items were generated by prompting a model with the easy tier and asking for harder variants, and the difficulty curve is lumpy in exactly the ways you'd expect from a model extrapolating its own training distribution rather than a human designing progression.
4. Wrapping up before the long weekend, so here's everything that moved. The ⟦TERM⟧ debate from a couple posts back finally got resolved — we're keeping the generated hard tier but relabeling it clearly instead of pretending a human wrote it, since ⟦X⟧ on it this week showed only a 3-point wording gap, tighter than I expected and honestly better behaved than some of our human-authored items.