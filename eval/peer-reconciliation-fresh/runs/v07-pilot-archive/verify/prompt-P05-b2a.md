Three DEFINITIONS of the same concept at increasing specificity:

DEFINITION L0 (genus only): A paired benchmark measure of how irrelevant prompt context affects an agent's navigation and exploration behavior during a task.

DEFINITION L1 (adds mechanism): A paired benchmark measure of how irrelevant prompt context affects an agent's navigation and exploration behavior during a task. It compares an agent's tool-call traces on two versions of the same task: a plain version and a version with irrelevant or distracting context added. The required action, verification endpoint or validator, and initial state stay the same in both versions.

DEFINITION L2 (adds measurement and conditions): A paired benchmark measure of how irrelevant prompt context affects an agent's navigation and exploration behavior during a task. It compares tool-call traces between a clean task prompt and a paired prompt containing irrelevant or distracting context, while keeping the required action or configuration change, verification endpoint or validator, and initial system state identical. It measures exploratory or extra tool calls and pages opened, time to the first valid tool call, the rate of direct validation calls, and navigation breadth. Results are read as differences between the distracting and clean versions, such as +2.1 pages, +38 seconds, +31% exploratory calls, and -18% direct validation calls. It applies to paired agent task benchmarks, including account-management and service-configuration tasks, and primarily captures changes in navigation and exploration rather than final answer length.

Below are numbered excerpts from a community's documents in which one term — masked as ⟦TERM⟧ — is used (other local jargon masked ⟦X⟧).

For EACH excerpt, and for EACH definition level INDEPENDENTLY, decide: is the referent of ⟦TERM⟧, as used in THIS excerpt, an instance of the concept described by THAT definition?

- "instantiates" — the usage is consistent with and exemplifies that definition.
- "contradicts" — the usage is incompatible with that definition (the referent has a property that definition excludes, or lacks one it requires).
- "insufficient" — this excerpt alone does not contain enough to decide for that definition.

A usage can instantiate L0 while contradicting L2 — judge each level on its own content. Judge each excerpt on its own text only. For L1 and L2, any decided verdict (instantiates or contradicts) requires a verbatim supporting quote from the excerpt; L0 needs no quote.

Output ONLY a JSON array, one object per excerpt:
[{"excerpt": 1, "L0": "instantiates|contradicts|insufficient", "L1": {"verdict": "...", "quote": "verbatim or empty for insufficient"}, "L2": {"verdict": "...", "quote": "..."}}, ...]

EXCERPTS:

1. Genuinely impressive, first agent on our roster to pass that cleanly. Less happy about our ⟦TERM⟧ results this week. Same underlying task, one variant phrased as a casual Slack message ("hey can you check this contract real quick"), the other as a formal request with numbered requirements.
2. We kept a suite alive for a year that stopped telling us anything for probably the last four months of it. Tried to console myself by running ⟦TERM⟧ on the surviving 39%, pairing each item with a reworded twin — same ask, different formatting, bullet list versus prose. Gap was smaller than I expected, only 4 points on average, which is actually reassuring since it means the items still doing work aren't just measuring who parses markdown better.
3. Half the "advanced" items were generated by prompting a model with the easy tier and asking for harder variants, and the difficulty curve is lumpy in exactly the ways you'd expect from a model extrapolating its own training distribution rather than a human designing progression. Ran ⟦TERM⟧ on that same tier to see if wording was compounding the problem — reworded ten items into terser, more casual phrasing and reran. Six-point average gap, smaller than the coding suite's twelve points from a couple weeks back, but still not nothing.
4. Wrapping up before the long weekend, so here's everything that moved. The ⟦X⟧ debate from a couple posts back finally got resolved — we're keeping the generated hard tier but relabeling it clearly instead of pretending a human wrote it, since ⟦TERM⟧ on it this week showed only a 3-point wording gap, tighter than I expected and honestly better behaved than some of our human-authored items.