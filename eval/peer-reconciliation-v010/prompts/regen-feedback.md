Your PREVIOUS attempt at the task below was REJECTED by an automatic validator. Regenerate a corrected output that fixes the stated problem while keeping everything else faithful to the excerpts.

WHY THE PREVIOUS ATTEMPT WAS REJECTED
- Failing gate: {FAILING_GATE}
- Measured value vs the cap it violated: {MEASURED_AND_CAP}
- What the cap requires: {CAP_IN_WORDS}

Fix ONLY the stated problem. Do not change parts of the output that were not flagged. Re-check your corrected output against the requirement above before emitting it. Output must be in exactly the format the task below specifies (same JSON schema / same line format), with no commentary about this correction.

=== ORIGINAL TASK (unchanged) ===
{BASE_PROMPT}
