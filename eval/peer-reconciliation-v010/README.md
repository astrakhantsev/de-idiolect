# peer-reconciliation-v010 — v0.10 generation-hardening run workspace

This is the implementation workspace that a frozen v0.10 pre-registration binds by hash. **Normative spec:** `/mnt/f/hub/10_projects/minelit/idiolect/2026-07-23-v010-generation-hardening-PREREG-DRAFT.md` (rev 10, FREEZE-CANDIDATE — workspace state after round-11 fixes: **166 tests, 97 fixtures, 2 verifier scripts, all green from a FRESH CHECKOUT**). Rounds 8–11 have been FOLDED into the workspace (the coordinator folds the matching spec-prose amendments into the ratified prereg at the freeze step). Round 11 closed the last custody/recovery bypasses: accidental-read `spent` records are never treated as recoverable (event_ref-inspected, fail-closed one-locked op); the scorer refuses a non-canonical custody-ledger override in runtime; the ONE benign attestation-1 re-freeze is recorded monotone in the canonical ledger and enforced cross-run (classification is one-shot); confirmatory exit codes are routed by class (only the ≤1/40 gate-fail retires); every issued setup attempt is recorded immediately with fresh-setup generations; an output-manifest construction refusal after generation forfeits; the setup manifest is discovered-set-exact and byte-anchored to the completion receipt; and confirmatory full-draw outputs are re-hashed at attestation. The round-8→10 hardening, cumulatively: per-H spend-log namespacing + a durable key-custody ledger + typed phase receipts + exact output-manifest set-equality + embargo/export split + attestation pre-claim binding + committed-toy-key/root-anchored-gitignore reproducibility (round 8); an IMMUTABLE frozen H + `generation-started` marker + one fail-closed ATOMIC custody claim + a post-confirmatory attestation-1 split + output-manifest closure + durable attempt-indexed setup reuse + all-`.md` corpora/key exactness + a pairs.json-bound projector receipt (round 9); and (round 10) the custody ledger moved to ONE canonical out-of-tree operator-local path with an explicit genesis + fail-closed availability, accidental reads transitioning custody, a persisted attestation-1 mismatch classification, the FULL confirmatory tool path per draw, the canonical freeze-manifest used directly in runtime, a complete per-key setup manifest, and corpora/key exactness enforced at BOTH attestation points. **DEFERRED (documented limitation):** full per-H directory namespacing of receipts/outputs — a revised prereg runs in a fresh checkout; single-run validity is unaffected. **Durable custody ledger (infrastructure, NOT repo content):** the cross-run key-3 custody ledger lives at the out-of-tree operator-local path `/mnt/f/hub/10_projects/minelit/idiolect/key3-custody.jsonl` (Syncthing-backed); seed it once with `python3 spend.py genesis`, and every run refuses before phase 0 if it is missing/unreadable. Section references (§) point to the spec unless prefixed v08/v09 (the frozen v0.9 instrument: `../peer-reconciliation-test3/prereg-v08.md`, `prereg-v09.md`). Where the spec defers byte-level behavior to "normative artifacts" (§3.6), the artifacts in this workspace ARE those definitions; prose describes intent, the frozen implementation + golden fixtures define behavior, and any divergence resolves in favor of the artifacts.

**Derivation:** everything is derived by COPYING from the frozen v0.9 harness and modifying the copies. No file outside this directory is edited. The gate-fidelity proof reads the frozen v0.9 reference DIRECTLY from the committed `../peer-reconciliation-test3/smoke.py` (there is no `_reference/` copy). The 7 unchanged pipeline prompts and the carried harness scripts are hash-verified against `../peer-reconciliation-test3/freeze-manifest.txt`; `run_calls.sh` was MODIFIED for §6 alias→pinned-ID translation at the boundary (it is NOT byte-identical to v0.9 — the gate/staging functions it drives are, proven by the fidelity script).

## Blindness constraints (restated — violating these destroys the experiment)

- The sealed answer material (`key/concepts.json`, `key/answer_key.json` of key-3) and the sealed run's per-pair records (`../peer-reconciliation-test3/runs/`) were NEVER opened during this build. Hardening rationale comes only from the spec + `2026-07-20-gate-stress-probes-RESULTS.md` (TRAIN-side diagnosis).
- **Answer-blindness (custody strengthening over v0.9; hardened in round-5).** The generation controller, the verification/composition layer, and both baselines read an **answer-blind `pairs.json`** = `{count, pairs: [{pair_id, term_a, term_b}]}` — the QUESTION (which term pairs to adjudicate), never the relation classes. Two answer-leak channels are closed: (1) `pair_id` is an **OPAQUE token** `sha256(term_a || NUL || term_b)[:16]` (UTF-8, NUL = the 0x00 byte) — NOT the positional `P01–P10` that would encode the class under the harness key schema; (2) the emit order is a **seeded-but-key-independent shuffle** (sorted by the opaque id, a uniform hash), so the sequence carries no `P01–P10` class ordering. (`term_a == term_b` for jingle pairs is inherent — the jingle strings are known/orchestrator-visible and are in the exposure ledger; it is not hidden.) The relation classes (`expected`/`broader_side`) live only in the sealed key and are read by **exactly one component**, `scorer_v010.py`, at the single authorized scoring read (the SPEND, §4.2 step 9). `pairs.json` is produced by `make_pairs_manifest.py` run as an **isolated subprocess** whose only output is the blind `pairs.json` + its hash (custody-logged, phase 0.5); the orchestrator never parses the key in-process. `load_pairs` enforces a strict whitelist schema, rejects symlinks, and rejects any resolved path under a `key/` directory. The scorer joins `pairs.json` ↔ sealed key on the **term pair** (opaque id has no positional meaning).
- No LLM calls were made during the build. All self-tests are offline/deterministic.

## Component map

| File | Role |
|---|---|
| `smoke_v010.py` | Hardened generation controller (copy of frozen `smoke.py`). H1 feedback-guided regen, H2 split regen budget (total 2 / semantic 1), H3+H4 via the base ladder prompt, answer-blind `load_pairs`. All GATE/validator/aggregation/composition functions carried byte-identical from v0.9 (proven by `scripts/verify_gate_fidelity.py`). |
| `v010.py` | Verification + composition layer (copy of frozen `v09.py`). τ machinery unchanged; NO carried stages (every call fresh); `compose` is ANSWER-BLIND — writes composed per-pair verdict records, does not grade. |
| `retrieve_xc_v010.py` | Cross-corpus retrieval (copy of `retrieve_xc.py`); algorithm byte-identical to v0.9; `--v010` + `--no-determinism` (key-3/confirmatory draws are fresh, first execution is the record). |
| `baseline_a.py` | Baseline-A (bare-coinage naive-ask, §3.4): 20 per-term isolated calls, term-as-query top-3 retrieval, parse + one re-ask + key-blind grounding -> records. |
| `baseline_b.py` | Baseline-B (equal-information direct ask, §3.5): 10 A→B isolated calls, `{a_excerpts}`+`{b_corpus}`, parse + one re-ask + key-blind grounding -> records. |
| `serializers.py` | The ONE definition of `{a_excerpts}`, `{b_corpus}`, and the Baseline-A top-3 doc block (§3.6d). |
| `assemble.py` | The ONE definition of assembled baseline prompt bytes (template + serializers), shared by the runners AND the conformance runner. |
| `parser_adjudicator.py` | The ONE total grammar over an adjudicator reply (§3.6a), shared by both baselines. |
| `scorer_v010.py` | **The ONLY key-reading component** (`score` + `export-embargo` subcommands). Counterpart-identity adapter + directional mapping + two-direction combination + coverage + §5 decision table + P=1.00 guardrail. Pre-claim it re-verifies key-file existence, the full output-manifest against the `output_manifest_sha256` bound in the attestation-2 receipt (`--attest2-receipt`, resolving relpaths under `--run-root`), then performs ONE `spend.atomic_claim` (custody `spent` + per-H claim together) at first read (the SPEND). Writes aggregate-only `scores.json` + an embargoed per-pair artifact. |
| `attest.py` | manifest-of-manifests **H** (§4.2; build-H REQUIRES `PREREG.md` + `recorded-cli.json` and binds their hashes; `--runtime` also requires the exact corpora/pairs/key), the reusable two-point attestation gate (STRICT verify-files + recorded-artifact hash-verify incl. isolation wrapper + live BGE recompute; round-8 pre-claim binding: sealed key-file existence/type, exact two-CLI `recorded-cli` schema + hash bound in H, point-2 probe-log identity vs point-1, and `--confirmatory` typed-receipt verification at point 1; `--recorded-cli`/`--probe-log` MANDATORY, integrity-bound into an attestation record), the EXACT step-7 output-manifest (set-equality), **typed phase receipts** (`phase-receipt`/`phase-verify`), `spend-log --H` (per-H) + `custody-log` (durable key-custody ledger), draw receipts carrying H. |
| `pin_model.py` | Frozen alias→pinned-ID translation at the call boundary (§6): `opus`→`claude-opus-4-8`, `sonnet`→`claude-sonnet-5`, pinned IDs pass through, any other Claude alias is rejected. `run_calls.sh` calls it before the frozen wrapper so every outcome-bearing Claude call is pinned WITHOUT editing the carried gates. |
| `PREREG.md`, `recorded-cli.json`, `REQUIRED-INVENTORY.txt` | Freeze-package files bound by hash in H. `PREREG.md` = ratified spec copy; `recorded-cli.json` = frozen CLI versions; `REQUIRED-INVENTORY.txt` = the CANONICAL EXACT implementation file list (build-H asserts the discovered impl set EQUALS it — rejects any extra or missing). Until `PREREG.md` is placed, build-H refuses (correct pre-freeze). |
| `runs/output-manifest.json` | The EXACT step-7 OUTPUT receipt (`attest build-output-manifest`): the expected set is DERIVED from the staged call manifests (completed call ⇒ its output expected; every manifest expected) + deterministic derived records, and enforced as SET EQUALITY (missing AND extra REFUSE the build), bound to H. Attestation-2 re-derives + re-checks; the scorer verifies its inputs against it BEFORE claiming the key. |
| `key-custody.jsonl` (runtime, gitignored) | Durable CROSS-RUN key-3 custody ledger (`eligible`/`forfeited-unspent`/`spent`); blocks scoring everywhere once spent/forfeited, independent of the per-H spend log. |
| `probe_explicit_id.sh` | Explicit-ID model probe (§6 step 3): asserts each pinned Claude ID is a member of the CLI's resolved set. **Do not run during build.** |
| `setup_confirmatory.py` | Setup-constructor wrapper (§4.1a): drives the UNCHANGED frozen v0.9 key-author/corpus path twice, attempt accounting (retry cap 1, hash all, none discarded, setup-exhaustion ≠ retirement). |
| `conformance_runner.py` | The §3.6(f) frozen conformance runner over `fixtures/*.json`; MUST PASS; part of both attestation points. |
| `make_pairs_manifest.py` | Answer-blind projection: key structure → `pairs.json` (pair_id/term_a/term_b only) + `--emit-receipt` (a projector receipt bound to the pairs.json hash) and a `--verify-receipt` mode the driver uses before registering `structure:read`. |
| `run_v010.sh` | The key-3 run driver (exact sequence; H1/H2 budget-2 loop). **Not executed during build.** |
| `run_confirmatory.sh` | Runs the tool path on one confirmatory key, counts configFails/40. **Not executed.** |
| `run_calls.sh` | v0.9 call executor, MODIFIED for §6 alias→pinned-ID translation at the boundary (not byte-identical to v0.9; the gate/staging functions it drives are). |
| `prompts/gen-definition-v010.md` | H3 (word-budget emphasis) + H4 (cumulative-extension) base ladder prompt (copy+modify of `gen-definition-v07.md`). |
| `prompts/regen-feedback.md` | H1 feedback regen template (`{FAILING_GATE}`,`{MEASURED_AND_CAP}`,`{CAP_IN_WORDS}`,`{BASE_PROMPT}`). |
| `prompts/baseline-a.md`, `baseline-a-reask.md`, `baseline-b.md`, `baseline-b-reask.md` | Literal baseline templates incl. the full re-ask bytes (§3.6b); Baseline-B's re-ask is its own file. |
| `prompts/{checklist-extract,ladder-conformance,polarity-check,verify-matrix,verify-pair,decompose,containment-v2}.md` | The 7 unchanged v0.9 pipeline prompts (hash-verified). |
| `harness/{keyspec-author.md,validate_key.py,build_briefs.py,gen_leakcheck.py,split_corpus.py}` | Frozen v0.9 key-author/corpus scripts (for the confirmatory setup only). `split_corpus.py` is now IN the workspace (hash e240…, verified vs the frozen record, bound in H); `setup_confirmatory.py` uses it by default. `gen-community-{a,b}.md` are NOT static files — `build_briefs.py` generates them per key (see round-5 note). |
| `fixtures/*.json` | Seed golden fixtures (28) defining the format for the fixture-oracle agent. |
| `toy-key/` | SYNTHETIC toy key + pairs.json + recorded-hashes for the scorer/phase-0 unit tests. No relation to any real key. Round-8: COMMITTED and bound in H (a fresh checkout reproduces phase 0); the `.gitignore` sealed-key patterns are ROOT-ANCHORED so they never clobber it. |
| `tests/test_v010.py` | Offline test suite (166 tests). |
| `scripts/verify_gate_fidelity.py`, `scripts/gen_seed_fixtures.py` | Gate byte-identity proof; seed-fixture generator. |

## Spec-section → implementation mapping

- **§2 H1 feedback-guided regen** → `smoke_v010._route_fail` + `write_regen_prompt` + `prompts/regen-feedback.md` (regen prompt receives failing gate identity, measured value, violated cap; per-generation-index prompt file).
- **§2 H2 / §5 regeneration state machine** → `smoke_v010._route_fail` counters `total_regens` (cap 2) / `semantic_regens` (sub-cap 1); mechanical+leak+generate share the total, semantic capped at 1 within it; max 3 generations (g0,g1,g2); configFail scoped to every pair involving the term. Mixed-class transitions tested.
- **§2 H3 word-budget emphasis + §2 H4 cumulative-extension** → `prompts/gen-definition-v010.md` (base ladder prompt; L2 60–160 enforced-cap emphasis; literal cumulative construction L1=L0+mechanism, L2=L1+metric/conditions). L2 cap stays 60–160 (Q6).
- **§2 gates unchanged** → proven by `scripts/verify_gate_fidelity.py` (60 gate/layer functions byte-identical to frozen v0.9).
- **§5 conformance batching** → `smoke_v010.prompts_conformance`: exactly one ordered batch per side per generation index (keyed by `total_regens`), terms in term order; one malformed re-run per batch; run-halt on still-malformed (`gate_conformance`, unchanged).
- **§3.4 Baseline-A** → `baseline_a.py` (retrieval unit = frozen v0.8 §2.6 verbatim, bare term query; per-term independent; truth table via the parser; both directions; combination in the scorer) + `prompts/baseline-a*.md`.
- **§3.5 Baseline-B** → `baseline_b.py` (`{a_excerpts}` 4–6 in pool order; `{b_corpus}` 11 files filename order; matched_term validation vs corpus) + `prompts/baseline-b*.md`.
- **§3.6(a) parser** → `parser_adjudicator.py`. **(b) templates + re-ask** → `prompts/baseline-*.md`. **(c) fixtures** → `fixtures/` (seed; extended by the fixture agent). **(d) serializers** → `serializers.py`. **(e) isolated single-turn** → each attempt staged as a new call through `run_calls.sh`→`../e2e-cell/run_isolated.sh`. **(f) conformance runner** → `conformance_runner.py`.
- **§4.1a confirmatory setup** → `setup_confirmatory.py`. **§4.1 confirmatory draws** → `run_confirmatory.sh`.
- **§4.2 H + attestation (2 points)** → `attest.py build-H` / `attest`. **§6 pinning** → `pin_model.py` (frozen alias→pinned-ID translation at the `run_calls.sh` boundary — the compliant implementation of "every Claude call by explicit pinned ID" that keeps the carried gates + the frozen wrapper byte-identical) + `probe_explicit_id.sh` (membership probe). **§4.4 spend-state / §4.3 key-3 state** → `spend.py` + `attest.py spend-log` (atomic-claim scoring transition, terminal-event markers, resume helper).
- **§5 scorer (adapter, coverage, decision table, P guardrail)** → `scorer_v010.py`.

## Exact run sequence (`run_v010.sh`; not executed here)

`projector → freeze → probe → setup → confirmatory draws → attestation(1) → generation → attestation(2) → scoring`

0. offline tests + gate fidelity + conformance runner (no model calls)
0.5. **PROJECTOR** (its source is already committed/immutable; runs BEFORE H so H can bind its output) — `make_pairs_manifest.py key pairs.json --recorded-hashes freeze-manifest.txt --emit-receipt runs/pairs-receipt.json`: hash-gates the sealed source file, emits blind `pairs.json` + `pairs_sha256:` + a **projector receipt** bound to the pairs.json hash (round-9 finding 7). Resume: skip if `pairs.json` + receipt are present.
1. FREEZE + H (**IMMUTABLE on restart**, round-9 finding 1): if `runs/H.json` exists the driver LOADS + self-verifies it (`verify-files` re-hashes the tree vs H) — a tree mismatch is an ATTESTATION FAILURE (HALT), NEVER a silent rebuild; only a first clean run calls `attest.py build-H --runtime` (REFUSES unless `PREREG.md` + `recorded-cli.json` + the exact corpora/pairs/key are present). **Cross-H generation-start guard:** if a durable `runs/generation-started.json` exists for a DIFFERENT H, an observed key-3 instance was already generating → HALT + demand classification (never overwrite). In-place new-H runs are UNSUPPORTED — a revised prereg runs in a FRESH checkout (DEFERRED: full per-H directory namespacing).
1.5. **structure:read custody** — the driver re-verifies the projector receipt vs the CURRENT `pairs.json` (`make_pairs_manifest.py --verify-receipt … --pairs pairs.json`; a tamper between projection and registration is REFUSED, round-9 finding 7), then `attest.py spend-log --event structure:read --H "$H"` (one-shot, per-H; resume-guarded by `spend.projector_completed(log, H)`) — the sanctioned NON-SPEND record, registered ON BEHALF OF the verified projector read.
2. `probe_explicit_id.sh` (2 calls; pinned-ID membership) → `runs/probe-log.json`
3. `setup_confirmatory.py` — 2 fresh TRAIN keys via the frozen v0.9 path (uses the in-workspace `harness/split_corpus.py`). The per-key `setup-key.done` receipt embeds the accepted-corpora hashes + `leakcheck_pass`; a restart re-hashes those corpora before skipping.
4. `run_confirmatory.sh` ×2 — tool path per conf key; gate ≤1 failed artifact/40 (a checklist configFail counts 2: the checklist AND its never-created ladder). A fail RETIRES the effective configuration/H (§4.1). The `confirmatory-result.json` receipt embeds H + `gate_pass` + the accepted-corpora hashes.
   **Resume + recovery (§10-F2):** every post-H phase (probe, each conf-key draw, attest-1, generation, baselines) writes a **TYPED** `runs/phase-*.done` receipt (`attest.py phase-receipt`) that binds H + the phase's required-output HASH set (+ semantic flags). On restart `attest.py phase-verify` re-hashes those outputs and re-checks the semantics before skipping — a stale/damaged receipt HALTS; a missing one re-runs the phase. `setup_confirmatory.py` writes DURABLE attempt-indexed snapshots+receipts IMMEDIATELY per site/attempt; a restart VERIFIES and REUSES a completed attempt (never re-issues the call), so recovery cannot create extra uncounted draws (round-9 finding 5). **No EXIT-trap auto-forfeit:** a pure infrastructure fault is RESUMABLE. Only two events consume key-3, each logged EXPLICITLY (persistence failure is a HARD HALT — no `2>/dev/null || true`, round-9 finding 2) + mirrored into the durable cross-run **key-custody ledger** (`key-custody.jsonl`): attestation-2 integrity mismatch → `state:terminated-during-gen-or-attest2-mismatch` + custody `forfeited-unspent`; a scorer fault after the claim → `spend:fault-after-authorized-read` (SPENT, custody recorded atomically at the claim). Eligible-outcome markers (`setup-exhaustion`, `confirmatory-phase-fail`) never touch the ledger. The spend log is **per-H namespaced**: an old-H terminal never blocks a new-H run.
5. ATTESTATION POINT 1 (**POST-confirmatory**, round-9 finding 3): STRICT verify-files (H self-checked first) + clean tree + **sealed key-file existence/type (no read)** + **exact two-CLI `recorded-cli.json` schema + its hash bound in H** + CLI-version enforced + probe membership + v0.9-carried-artifact hash-verify (incl. isolation wrapper) + live BGE recompute + conformance runner + **both confirmatory typed receipts verified** (`--confirmatory`); + receipt. A failure here is a POST-confirmatory **ATTESTATION-1 MISMATCH** → **HALT + demand classification** (NOT silently resumable): a benign/non-configuration mismatch requires a **re-freeze + two NEW draws (once)** in a fresh instance (observed draw receipts are NOT reusable across a re-freeze — new freeze = new H = new receipts); a configuration mismatch RETIRES the configuration. (Pre-confirmatory aborts — projector/probe — are the resumable ones, handled at their own phases.)
6. key-3 tool-arm generation (answer-blind): excerpts → checklists (H1/H2 loop) → ladders (H1/H2 loop) → conformance (DRAIN ladder regens to quiescence before each lowest-pending-index batch — no stranding) → polarity → retrieval → verify → aggregate → adaptive-1/2 → compose
7. baselines (answer-blind): A retrieve/prompts/gate(+reask), B prompts/gate(+reask)
8. **step-7 OUTPUT MANIFEST** (`attest build-output-manifest`): derives the EXACT expected step-7 set from the staged call manifests (a completed call — `exit:0` + recorded `out_sha256` — expects its output regardless of whether the file still exists, so a deleted completed output is CAUGHT; every call manifest is expected) + the deterministic derived records (**required UNCONDITIONALLY** — a deleted derived record refuses, round-9 finding 4), and enforces **SET EQUALITY** (missing OR extra refuses) PLUS a **completed-output drift check** (every present completed output must byte-hash its call manifest's `out_sha256`). Attestation-2 re-derives and re-checks all of this, and BINDS `output_manifest_sha256` into its receipt.
9. **THE SPEND (fresh attest-2 per attempt + ATOMIC claim + one bounded relaunch):** each attempt runs ATTESTATION POINT 2 FRESH (verify-files + recorded-artifacts + BGE + sealed key existence + `recorded-cli` schema/hash + **the same probe-log bytes recorded at attestation-1** + the exact output-manifest; binds `output_manifest_sha256`), then appends a bounded per-H `state:scoring-attempt` (max 2) with an EXPLICIT `|| exit`. `scorer_v010.py score` runs the pre-claim gate (per-H spend log + the cross-run custody ledger, **recovery-aware**: a same-H spent state is a recovery case), re-checks **key-file existence**, verifies the output-manifest bytes hash-match the value bound in the attestation-2 receipt, and re-verifies **EVERY** file in the manifest present + hash-match (round-9 finding 4 — catches a manifest+input swapped between attestation-2 and the claim), then performs ONE **fail-closed atomic claim** (`spend.atomic_claim`: under the custody-ledger lock THEN the spend-log lock, re-check availability + the per-H gate, append custody `spent` and the per-H `authorized-read-claimed` together — idempotent on a crash between the two writes) immediately before the first key byte, reads the key, writes **AGGREGATE-ONLY** `runs/scores.json` + an **EMBARGOED** per-pair artifact (hash-bound into scores.json, never released here), and appends `spend:authorized-read-complete`. A pre-read failure with attempts<2 → ONE relaunch (re-attesting first); a failure AFTER the claim → `spend:fault-after-authorized-read` (SPENT), no relaunch; after the relaunch → `state:terminated-during-gen-or-attest2-mismatch` + custody forfeit. Per-pair diagnostics are released ONLY by the separate NO-KEY exporter `scorer_v010.py export-embargo --repo … --commit …`, which verifies a REAL post-commit addendum (the commit's tree carries the E5.1 marker).

## Interfaces the fixture-oracle agent needs

- **Parser entry point:** `parser_adjudicator.parse_adjudication(text: str, relation_enum: set) -> {"classification": "positive"|"negative"|"malformed", "reason": str, "fields": {"match","matched_term","relation","evidence"}}`. Arm enums: `RELATION_ENUM_A`, `RELATION_ENUM_B`; helper `enum_for_arm("A"|"B")`.
- **Serializer signatures** (each returns pure content — NO leading/terminal newline; the template owns separators + the single terminal newline; every item incl. the first is labeled):
  - `serializers.serialize_a_excerpts(excerpts: list[str]) -> str` (requires 4≤len≤6; `"N. <text>"` joined by `\n`)
  - `serializers.serialize_b_corpus(docs: list[(label,text)]) -> str` (`"=== DOCUMENT <label> ===\n<rstripped text>"` joined by `\n\n`)
  - `serializers.serialize_baseline_a_docs(ranked_docs: list[(label,text)]) -> str` (`"--- RANK <n> (document <label>) ---\n<text>"` joined by `\n\n`)
  - `serializers.concat_docs_text(docs) -> str` (general concatenation utility; NO longer the grounding path — grounding is per-single-document, see BUG-1 fix)
- **Assembly signatures:** `assemble.assemble_baseline_a(term, ranked_docs, reask=False) -> str`; `assemble.assemble_baseline_b(term_a, excerpts, docs, reask=False) -> str` (single-pass substitution — data containing placeholder tokens is inserted verbatim).
- **Template placeholder names:** Baseline-A `{TERM}` `{RETRIEVED_DOCS}`; Baseline-B `{TERM_A}` `{A_EXCERPTS}` `{B_CORPUS}`; ladder `{CHECKLIST}`; regen `{FAILING_GATE}` `{MEASURED_AND_CAP}` `{CAP_IN_WORDS}` `{BASE_PROMPT}`.
- **Fixture format + runner:** each fixture is a JSON object `{"kind", "name", "why", ...inputs, ...expected}`; kinds `parse | serialize_a_excerpts | serialize_b_corpus | serialize_baseline_a_docs | assemble_baseline_a | assemble_baseline_b | ground | pairs_manifest` (schemas in `conformance_runner.py`). `pairs_manifest` pins the §3.6 answer-blind pairs.json schema: `{"term_pairs": [[a,b],...], "expect": {count, pairs}}` — `build_payload` must yield top-level `{count, pairs}`, per-record exactly `{pair_id (16-hex), term_a, term_b}`, `pair_id = sha256(term_a‖NUL‖term_b)[:16]`, sorted by `pair_id`. `python3 conformance_runner.py` runs all `fixtures/*.json` and MUST exit 0. Seed fixtures are labeled `"oracle": "independent"` (hand-specified) or `"regression-seed"` (impl-derived bytes — the fixture agent should replace these with independent byte oracles for full §3.6(c) coverage).
- **Scorer entry:** `scorer_v010.py --key-dir <dir> --recorded-hashes <file> --pairs <pairs.json> --H <runs/H.json> --spend-log <log> [--tool-verdicts ...] [--baseline-a ...] [--baseline-b ...] --out <scores.json>`. The key path is a parameter and never resolves during build/test. The scorer checks the locked spend gate, verifies H self-consistency, binds `--recorded-hashes` to H, and verifies sealed recorded hashes before the SPEND.
- **Grounding helper (arm-specific, key-blind):** `baseline_a._ground(fields, docs)` / `baseline_b._ground(fields, docs)` — each field grounded iff it is a substring of at least one SINGLE document (BUG-1 fix); `docs` = list of `(label, text)`.

## Deviations from the spec / judgment calls (feed round-5)

1. **Answer-blind `pairs.json` split (the load-bearing custody decision) — hardened in round-5.** The spec requires the scorer to be the only component referencing the sealed key path, and step-7 generation to be "sealed-answer-material-blind," yet retrieval + verify + adaptive stages structurally need the *pairing*. Resolution: the pairing is the QUESTION (answer-blind), supplied via `pairs.json` with **opaque `sha256(terms)` ids in a key-independent shuffle** (no `P01–P10` positional class encoding) and a **strict-whitelist, symlink-safe** `load_pairs`; the ANSWER (`expected`/`broader_side`) is read only by the scorer, which joins on the term pair. The projection runs as an **isolated subprocess** (phase 0.5) emitting only the blind `pairs.json` + hash, custody-logged; the orchestrator never parses the key in-process. **Residual flagged for round-5:** for key-3 the isolated projector still *parses* the sealed `concepts.json` (which contains classes/descriptions) to extract the terms+pairing, discarding the answer — a bounded, logged read, not the SPEND. If round-5 judges even the isolated parse unacceptable, the blind `pairs.json` must be emitted by the isolated key-author at seal time instead.
2. **Parser wrapper-quote stripping (refined in round-5).** The parser strips at most one **genuinely matched** pair of surrounding double quotes — ASCII `"…"` OR curly `“…”`; **mixed** wrappers (`"…”`, `“…"`) are NOT stripped. Pinned by 4 fixtures (2 matched-strip, 2 mixed-not-stripped). A stricter "no stripping at all" variant remains possible; flagged.
3. **Conformance batch key = generation index** (not v0.9's incrementing wave counter). Implements §5's "one ordered batch per side per artifact-generation index" literally; equivalent to v0.9's waves under the driver's sequencing, but keyed to the spec's unit.
4. **`split_corpus.py` is now IN the workspace** (round-5): copied from the frozen v0.9 tree, hash-verified against the freeze-manifest recorded value (e240…), bound in H, and used by `setup_confirmatory.py` by default (the earlier operator-supplied `--split-script` deviation is dropped). **Correction to the round-5 review re `gen-community-{a,b}.md`:** these are NOT generic static files and were NOT added. `build_briefs.py` (frozen, in H) generates them PER KEY from that key's `concepts.json`; the confirmatory path generates fresh ones per conf key, and the key-3 v0.10 path does not regenerate corpora (they are frozen precondition inputs), so no static `gen-community` file is referenced. The key-3 `gen-community-{a,b}.md` recorded as 967…/22e9… in the freeze-manifest are the SEALED, key-3-specific briefs (they embed key-3's concept descriptions per `build_briefs.py`), so copying them would breach blindness — they are deliberately not in the workspace.
5. **`retrieve_xc_v010.py --no-determinism`.** Key-3 and each confirmatory draw are fresh (no prior v0.8 retrieval baseline), matching the frozen key-3 TEST harness which added the same flag; the retrieval algorithm is byte-identical to v0.9.
6. **Confirmatory draws run in a copied workspace** (`run_confirmatory.sh` copies the instrument into each conf-key dir), matching the frozen precedent (test3 = the v0.9 package copied verbatim per key), so no in-place workspace parameterization was added (that would have broken the gate byte-identity proof).

## What is tested offline vs untested

**Tested (deterministic, no model/venv):** extended gate byte-identity (63 carried functions + 11 module globals + imports, reference hash-bound to the frozen record); the H1/H2 state machine (split budget, semantic sub-cap, both mixed-class orders, max-3, leak shares budget, generate-malformed non-semantic, H1 feedback-prompt content); conformance batching — only the lowest-pending index staged, and a late g0→g1 entrant COALESCES with a mechanical g1 entrant (no stranding; `assert_resolved` passes); the full parser grammar (every branch) incl. matched-vs-mixed wrapper quotes; serializers at 4/5/6 + first-doc labeling + terminal-newline + rstrip; single-document grounding + single-pass substitution; the scorer (adapter, mapping, all combination rows, coverage, decision table, sealed-hash custody, H self-check tamper, opaque term-pair join); the **atomic-claim spend** (claim one-shot; scoring-attempt capped at 2; one pre-read failure then relaunch; crash-after-claim refuses a second invocation; zero structure-reads refused; untyped/answer-read refused; accidental blocks all); opaque-id shuffle blindness; strict-whitelist + symlink + under-`key/` + slug-collision rejection; **strengthened attestation** (STRICT verify-files fails on missing/extra runtime + missing corpus; recorded-artifact hash-verify incl. isolation wrapper vs the frozen record; live BGE-tree recompute + mismatch detection); `attest build-H` determinism + `recorded_manifest_sha256` binding; the fidelity proof catching a mutated global; the projector hash gate (abort with nothing read on mismatch) + the driver-logged per-H `structure:read`; the `pairs_manifest` §3.6 schema; **executable confirmatory setup** (the per-key hash-verified splitter writes 11 docs UNDER the key dir + the frozen cross-side/meta leak checks pass, and detect an injected cross-side leak); **alias pinning** (`pin_model.py` translates opus/sonnet→pinned, rejects other Claude aliases; every staged Claude model is pinnable; `run_calls.sh` translates at the boundary); **spend/driver closure** (fault-after-claim requires a prior claim ⇒ post-claim = spent; the projector-completed resume helper; terminal markers always appendable; the driver wires the same classifier to both scorer attempts + all terminal events); **H freeze-package completeness** (build-H refuses without `PREREG.md`; deterministic with the freeze-package present + binds `prereg_sha256`/`recorded_cli_sha256`; `--runtime` refuses without corpora). **Round-7 additions:** the scoring-attempt cap (a third attempt is REFUSED before the scorer — shell-level regression) and the gates rejecting any terminal state; per-key confirmatory idempotence (`build_key` skips a completed key, refuses to overwrite under a different H); the output-manifest binding (the scorer aborts PRE-CLAIM on a tampered or missing input — unspent); exact inventories (`impl_inventory_diff` detects extra/missing; `corpora_exact_errs` rejects `corpora/a/12.md` and a missing doc); AGGREGATE-ONLY `scores.json`; the conformance-drain e2e (semantic-fail → regen → mechanical-fail → second regen → conformance, driven through the real gate loop, nothing stranded); corpus attempt hygiene (a misnumbered 00–10 attempt rejected; a retry after an extra-`12.md` attempt promotes exactly 01–11 with no stale file). **Round-8 additions:** per-H terminal isolation (an old-H terminal does not block a new H; eligible-outcome markers do not block a same-H resume) + the durable custody ledger blocking scoring cross-run; typed phase receipts (roundtrip / missing→run / stale→HALT / damaged-output→HALT / semantic-assertion) + `phase-receipt` refusing to write when a required output is absent + the setup/confirmatory typed skips re-hashing accepted corpora; the EXACT output-manifest set-equality (a deleted completed output and an unlisted extra output both REFUSE build/attestation); confirmatory typed-receipt verification (H / gate_pass / corpora-drift / missing); attestation pre-claim input binding (key existence, exact CLI schema, `recorded-cli` hash vs H, point-2 probe-log identity) + the scorer refusing pre-claim on absent key files; the embargo-then-export-with-real-addendum-commit path (fabricated commit refused, real E5.1 commit releases). **Round-9 additions:** the ONE fail-closed atomic custody claim (writes ledger+claim then refuses a second; crash-after-ledger-before-claim recovery is idempotent; concurrent different-H refused) + idempotent/monotone `record_custody` + recovery-aware `assert_key_available`; the `generation-started` marker; setup completed-attempt REUSE on restart (no re-issue); the projector-receipt tamper refusal; output-manifest closure (unconditional derived records, completed-output drift refused, and a manifest+input swapped between attestation-2 and the claim refused by the scorer via the bound `output_manifest_sha256`); all-`.md` corpora + key-dir exactness (nonnumeric extra + unexpected key file); and the driver text (immutable-H verify-not-rebuild, cross-H generation-start HALT, attestation-1 HALT-classify, no error-suppression). **Round-10 additions:** the out-of-tree custody model (genesis seeds eligible once + refuses re-seed; a MISSING ledger refuses fail-closed; a fresh-checkout instance still sees a key spent ELSEWHERE via the out-of-tree ledger and refuses; an accidental answer read transitions custody `spent`); the driver preflight refusing without a genesis ledger (`--test`); attestation-1 mismatch persistence (pending-classification terminal blocks the H) + the `classify-attest1` recorder; the confirmatory FULL-tool-path wiring (offline stage-plan grep: setup-manifest verify + polarity/retrieval/verify/aggregate/adaptive-1/2/compose + embedded stage flags) with the ≤1/40 gate still generation-only; confirmatory receipt verification now requiring all full-draw stage flags + the complete setup manifest (a tampered pairs.json/leakcheck refuses at attestation-1); and corpora/key exactness enforced at both attestation points. **Round-11 additions:** accidental `spent` never recoverable (event_ref-inspected; both crash-between-writes orders refuse) + the accidental transition still spends custody; the scorer refusing a runtime non-canonical `--custody-ledger` override; one-shot attest-1 classification + the ONE cross-run benign re-freeze (`benign-refreeze-used` monotone, a second benign refused, and it is not a custody state); setup no-output attempts recorded immediately + not reissued beyond the cap + fresh-setup generations preserving prior evidence; confirmatory receipt now binding the full-draw output HASHES + the setup-manifest bytes hash (drifted full-draw artifact / regenerated manifest / unlisted extra all refuse at attestation-1); and the driver text (confirmatory exit-class routing, output-manifest-failure forfeit, runtime scorer with no ledger override). **Per-kind fixtures (97):** parse 57, ground 15, serialize_a_excerpts 5, serialize_b_corpus 5, serialize_baseline_a_docs 4, assemble_baseline_a 3, assemble_baseline_b 5, pairs_manifest 3. **166 tests + 97 conformance fixtures + 2 verifier scripts, all green — reproduced from a FRESH CHECKOUT (clean copy of only the committed files + the committed test3 siblings).**

**Untested offline (require LLM CLIs / the BGE venv / real corpora / the sealed key — exercised only at run time):** any actual model call; retrieval execution (BGE embeddings) for the tool arm and Baseline-A; the end-to-end generation loop against real corpora; `probe_explicit_id.sh`; `setup_confirmatory.py` non-`--dry-run`; `run_confirmatory.sh`'s copied-workspace tool-path run; the `attest attest` clean-tree / CLI-version-enforce / probe-membership checks; the scorer's read of the *real* sealed key (only the toy key is exercised).

## Spec-wording amendments implied for the freeze fold (running list)

These are artifact-vs-prose divergences the artifacts resolve; the frozen spec text should be amended to match at the freeze fold (artifacts are normative, §3.6):

1. **Single-document grounding** — §3.4/§3.5's "a contiguous substring of the concatenated retrieved documents" → "a contiguous substring of at least one individual retrieved document" (a boundary-spanning span validates in no single document and must fail; per the BUG-1 fix).
2. **Checklist-exhaustion counting** — §4.1's confirmatory numerator counts a checklist configFail as **2** failed artifacts (the checklist AND its never-created ladder) against the fixed denominator 40.
3. **Projector custody step** — the answer-blind `pairs.json` is produced by an isolated-subprocess projector (phase 0.5), bound in H, before generation; `pair_id` is an opaque hashed token in key-independent shuffled order (no `P01–P10` positional class encoding). The projector HASH-GATES the sealed source file against the recorded value in the committed `freeze-manifest.txt` before parsing (abort with nothing read on mismatch). **Round-8:** the single typed `structure:read` (NON-SPEND) custody entry is logged by the DRIVER at phase 1.5 (`attest.py spend-log --event structure:read --H "$H"`), NOT by the projector — it must carry the run's H, which does not exist at the pre-H phase-0.5 read. The pairs.json schema is itself a §3.6 committed artifact, fixture-backed (`pairs_manifest` kind). The spec's custody wording (§4.3/§4.4) should name the `structure:read` typed entry as a sanctioned non-spend reader alongside the scorer's authorized read.
4. **Opaque-id formula** — the blind-manifest `pair_id` is `sha256(term_a || NUL || term_b)[:16]` (UTF-8, NUL byte 0x00); §3.6(d′) and this README's top section both use the NUL form (the delimiter-free `term_a || term_b` wording in the spec should be corrected to match).
5. **Projector-before-H ordering** — §4.2 must renumber Phase 0.5 BEFORE the H-building step and state that the projector *source* is already committed/immutable (so it runs first, then H binds both that source and its `pairs.json` output). The workspace already runs `projector → build-H`.
6. **Parser field order** — §3.6(a) says "exact field set and order," but the fixtures deliberately pin ORDER-AGNOSTIC parsing (`parse_field_order_reversed_*` expect success); §3.6(a) should read "exact field set, order-agnostic" (the prompt still states a preferred order). No code change — the artifact already parses any order.
7. **Atomic-claim spend transition** — §4.4 must describe scoring as: driver `state:scoring-attempt` (max 2) → scorer `spend:authorized-read-claimed` under the lock immediately before the first key byte → `spend:authorized-read-complete`; a pre-read failure with attempts<2 permits ONE relaunch, a claim (or attempts==2) forbids any further read; a post-claim fault is `spend:fault-after-authorized-read` (spent+invalid), never forfeited-unspent.
8. **Pinning-at-boundary (§6)** — "every Claude call by explicit pinned ID" is implemented by a frozen alias→pinned-ID translation at the `run_calls.sh` boundary (`pin_model.py`), which keeps the carried gate/staging functions and the frozen isolation wrapper byte-identical; §6 should name translation-at-boundary as the compliant mechanism.
9. **Freeze-package binding (§4.2/§8)** — H binds the ratified `PREREG.md` BYTES (`prereg_sha256`) and `recorded-cli.json` (`recorded_cli_sha256`), not a textual spec label; build-H enforces a REQUIRED inventory (refuse on any missing required file; `--runtime` = exact corpora 01..11 + pairs.json + leakcheck + key); attestation binds the `recorded-cli.json` + probe-log hashes into an integrity record and requires both inputs.
10. **Confirmatory leak execution (§4.1a)** — setup EXECUTES the frozen `leakcheck_peer.sh cross-a|cross-b|meta` over every corpus doc as part of corpus-attempt validation (a leak consumes the attempt), exactly as `run_test_v09.sh` does; and runs a per-key hash-verified `split_corpus.py` so outputs land under the key dir.
11. **Scoring relaunch + re-attestation (§4.4)** — the ONE permitted pre-read relaunch runs attestation-point-2 FRESH first; the attempt marker is capped at 2 with an executable refusal BEFORE the scorer (a restarted third attempt cannot reach it); the claim/scorer gates require a uniquely-open in-range attempt and reject any terminal state.
12. **Restart/terminal routing (§4.2/§4.3) — superseded by amendment 16.** H-bound phase receipts skip verified-complete phases on restart and never regenerate a completed confirmatory key/draw. (Round-8 revises the routing: infra faults are resumable, not forfeited; see amendment 16.)
13. **Step-7 output manifest (§4.2 steps 7–9) — refined by amendment 18.** The step-7 output set (scorer inputs + model outputs) is hashed into a receipt bound to H, enforced at attestation-2, and re-verified by the scorer against every input BEFORE the claim (a missing/drifted input aborts pre-claim, unspent). Round-8 makes it EXACT (set-equality; amendment 18).
14. **Exact inventories (§4.2/§8)** — build-H enforces set-EQUALITY against a canonical `REQUIRED-INVENTORY.txt` for implementation files (rejects any extra or missing) and against the exact 01..11 corpora under `--runtime` (rejects e.g. `corpora/a/12.md`).
15. **Aggregate-only scoring output (§3.3 Q7 / §4.2 step 10) — export path revised by amendment 19.** `scores.json` carries aggregates only (S, P, C, decision row, aggregate FP/promotion counts — no per-pair/expected/pair-id data). (Round-8 replaces the `--post-commit-receipt` export with an embargo-during-read + a separate NO-KEY exporter; see amendment 19.)

**Round-8 amendments (recovery, custody, receipts, exactness, attestation binding, reproducibility):**

16. **Per-H recovery + durable key-custody ledger (§4.1/§4.2/§4.3).** The spend log is PER-H NAMESPACED: every event records `run_H` and the gates evaluate only the current H's events, so an old-H terminal never blocks a new (revised-prereg) H. A pure infrastructure fault is RESUMABLE — the driver has NO EXIT-trap auto-forfeit; it exits nonzero and a re-run resumes via the typed phase receipts. Only two events consume key-3 and each is logged EXPLICITLY and mirrored into a durable CROSS-RUN key-custody ledger (`key-custody.jsonl`, states `eligible`/`forfeited-unspent`/`spent`): attestation-2 integrity mismatch → `terminated-during-gen-or-attest2-mismatch` + `forfeited-unspent`; a scorer fault after the claim → `fault-after-authorized-read` (SPENT, recorded at the claim). Eligible-outcome markers (`abort-before-gen`, `setup-exhaustion`) are documentation only and never block a same-H resume; `confirmatory-phase-fail` retires this H but leaves the key eligible for a different H (ledger untouched). The custody ledger blocks scoring everywhere once spent/forfeited.
17. **Typed phase receipts (§4.2/§10-F2).** Each post-H phase writes a TYPED receipt binding H + the phase's required-output HASH set (+ semantic flags, e.g. confirmatory `gate_pass=true`, setup accepted-corpora + `leakcheck_pass`). A skip requires `attest.py phase-verify` to re-hash the required outputs and re-check the semantics; a stale/damaged receipt HALTS (never skips a missing/corrupted phase), a missing receipt re-runs the phase.
18. **Exact step-7 output manifest (§4.2 steps 7–9).** The expected step-7 set is DERIVED from the staged call manifests (a completed call — `exit:0` + recorded `out_sha256` — expects its output regardless of whether the file still exists, so a deleted completed output is CAUGHT; every call manifest is expected) plus the deterministic derived records, and enforced as SET EQUALITY at build AND attestation-2: a missing expected output OR an unlisted extra output is REFUSED.
19. **Post-commit embargo/export split (§3.3 Q7 / §4.2 step 10).** During the single authorized read the scorer writes an EMBARGOED per-pair artifact and binds its hash into the aggregate-only `scores.json`; it is NOT released. A separate NO-KEY exporter (`scorer_v010.py export-embargo`) releases it only after verifying a REAL post-commit addendum (the named commit's tree carries the E5.1 marker). The scorer never re-enters the spend gate post-commit.
20. **Attestation pre-claim input binding (§4.2 steps 6/8).** Both attestation points check sealed key-file EXISTENCE/type without reading contents, require the EXACT two-CLI schema in `recorded-cli.json` and that its bytes hash-match the value bound in H, and require attestation-2 to use the SAME probe-log bytes recorded at attestation-1; attestation-1 additionally verifies both confirmatory draws' typed receipts. The scorer likewise refuses pre-claim if the key files are absent.
21. **Reproducibility (§8).** The synthetic `toy-key/` is COMMITTED and bound in H (a fresh checkout reproduces phase 0), and the `.gitignore` sealed-key patterns are ROOT-ANCHORED (`/key/`, `/pairs.json`, …) so they never clobber `toy-key/`. The gate-fidelity proof derives the frozen v0.9 reference DIRECTLY from the committed `../peer-reconciliation-test3/smoke.py` (hash-checked vs `freeze-manifest.txt`); the former `_reference/` copy is REMOVED. Verified by a FRESH-CHECKOUT battery (fidelity + conformance + all tests green from a clean copy).

**Round-9 amendments (H immutability, atomic custody, attestation-1 split, manifest closure, setup durability, corpus/key exactness, structure-read attribution):**

22. **Immutable frozen H + generation-start marker (§4.2/§4.3).** The driver NEVER rebuilds `runs/H.json` if it exists — it loads and self-verifies it (a tree mismatch is an attestation failure, HALT, not a rebuild). A durable `state:generation-started` marker (spend log + `runs/generation-started.json`) is persisted before the first key-3 generation call; a startup that finds it for a DIFFERENT H than the loaded one HALTS and demands classification (never proceeds, never overwrites). In-place new-H runs are UNSUPPORTED — a revised prereg runs in a FRESH workspace checkout. **DEFERRED (documented limitation):** full per-H directory namespacing of receipts/outputs/confirmatory dirs; single-run validity is unaffected.
23. **One fail-closed atomic custody claim (§4.4).** `spend.atomic_claim` locks the custody ledger THEN the spend log (fixed order), re-checks availability + the per-H gate, and appends custody `spent` and the per-H `authorized-read-claimed` together before releasing. `record_custody` refuses a conflicting transition and treats a same-H duplicate spent/forfeited append as an idempotent no-op (recovery after a crash between the ledger write and the claim proceeds and appends only the missing claim; a prior claim — key already read — is refused). Persistence of a state/custody transition is a HARD HALT on failure (no error suppression). A concurrent different-H scorer is refused by the ledger.
24. **Attestation-1 behavior split (§4.2 step 6 / §4.3).** Pre-confirmatory aborts (probe/projector/infra) are same-H resumable via receipts; a POST-confirmatory attestation-1 MISMATCH HALTS with a classification demand — benign/non-configuration → re-freeze + two NEW draws (once), configuration → retirement. The driver never silently re-runs attest-1 as resumable and never reuses observed draw receipts across a re-freeze (new freeze = new H = new receipts).
25. **Output-manifest closure (§4.2 steps 7–9).** Deterministic derived records are required UNCONDITIONALLY (a deleted one refuses, not omits); every completed staged output is byte-hash-compared to its call manifest's `out_sha256` at build AND attestation-2 (a modified completed output refuses); `output_manifest_sha256` is bound into the attestation-2 receipt and the scorer re-verifies the FULL manifest (not just its three inputs) against that bound hash immediately before the claim (a manifest+input swapped between attestation-2 and the claim is refused).
26. **Setup attempt durability (§4.1a).** Every site/attempt gets a UNIQUE durable output+manifest+receipt path written IMMEDIATELY (attempt-indexed); a restart VERIFIES and REUSES a completed attempt, never re-issues the call, and preserves exhausted-attempt evidence — so infrastructure recovery cannot exceed the frozen attempt caps.
27. **Corpus/key exactness (§4.2/§8).** Exactness compares the COMPLETE discovered `corpora/{a,b}/*.md` set (ALL `.md`, catching a nonnumeric `corpora/a/notes.md`) against the canonical `01..11`, and the discovered `key/*` set against exactly `{concepts.json, answer_key.json}` (an unexpected key-dir file is caught).
28. **Structure-read attribution (§4.2 step 2b).** The projector emits a receipt bound to the projected `pairs.json` hash; the driver registers `structure:read` (after H) ON BEHALF OF that receipt, re-hashing `pairs.json` at registration — a tamper between projection and registration is refused, and registration without a verifiable receipt is refused.

**Round-10 amendments (out-of-tree custody survival, attestation-1 persistence, full confirmatory protocol, canonical manifest, setup-manifest completeness, key-dir exactness at attestation):**

29. **Cross-run custody survival (§§4.3–4.4).** The key-3 custody ledger lives at ONE canonical, out-of-tree, operator-local path (`/mnt/f/hub/10_projects/minelit/idiolect/key3-custody.jsonl`, Syncthing-backed — infrastructure, not repo content). An explicit eligible GENESIS record is required (`spend.py genesis`, one-shot, refuses re-seed); every run REFUSES before phase 0 if the ledger is missing/unreadable/un-seeded; a MISSING ledger never reads as eligible (fail-closed). Runtime mode fixes the path (no env override; `--test` gates overrides for the offline suite). ALL custody transitions route through the one fail-closed API: authorized reads (`atomic_claim`), ACCIDENTAL reads (`append_event` for `spend:accidental-access-*` now ALSO records custody `spent`, blocking every later instance), and forfeitures.
30. **Attestation-1 mismatch persistence (§4.2 step 6 / §4.3).** A post-confirmatory attestation-1 mismatch persists a typed `state:attest1-mismatch-pending-classification` terminal (spend log + receipt) BEFORE exiting; a startup guard refuses that H while it stands; `spend.py classify-attest1 benign|configuration` records the operator's resolution (benign ⇒ a NEW freeze instance in a fresh checkout; configuration ⇒ retirement) — either way THIS H never proceeds.
31. **Full confirmatory tool path (§§4.1, 7).** A QUALIFYING confirmatory draw (≤1/40 generation gate unchanged) runs the FULL tool path — polarity → retrieval → verify → aggregate → adaptive-1/2 → composition — reusing the same phase functions as the key-3 driver, pointed at the conf key, exercising H end-to-end (~87 calls/draw). The `confirmatory-result.json` receipt embeds full-draw stage-completion flags; attestation-1 requires them all true.
32. **Canonical freeze manifest (§4.2/§8).** Runtime build-H uses the committed `../peer-reconciliation-test3/freeze-manifest.txt` DIRECTLY (no caller-selectable path); build-H refuses a `--recorded-manifest` that is not byte-equal to the committed canonical, and the driver refuses a local copy that differs. `--test` permits a toy manifest for the offline suite.
33. **Complete per-key setup manifest (§4.1a).** Each conf key emits one exact `setup-manifest.json` covering source scripts, generated briefs, all attempt outputs/manifests/receipts, accepted corpora, `pairs.json`, `leakcheck_peer.sh`, and key-derived records; it is re-verified on restart, before each draw (`setup_confirmatory.py --verify-setup`), and at attestation-1 (`_verify_confirmatory_receipt`). A tampered `pairs.json`/leakcheck after setup refuses at attestation-1.
34. **Key-dir exactness at attestation (§4.2 step 6).** The complete corpora/key exact-set check (`corpora_exact_errs`, incl. `key_extra`) runs at BOTH attestation points, not only build-H — a gitignored extra key file added after H is rediscovered and rejected.

**Round-11 amendments (accidental-read recovery, scorer ledger fixing, one-time benign re-freeze, confirmatory exit routing, setup generations, output-manifest custody, setup-manifest anchoring, full-draw re-hash):**

35. **Accidental-read recovery closed (§4.4).** An accidental-access answer read routes through ONE fail-closed locked op (custody `spent` FIRST — event_ref = the accidental event — then the per-H event), so a crash between the writes still blocks EVERY instance. Same-H `spent` recovery is permitted ONLY when the record's `event_ref` is `spend:authorized-read-claimed` (a crash inside `atomic_claim`); an accidental `spent` is NEVER recoverable.
36. **Scorer custody-ledger fixed in runtime (§4.4).** The key-reading scorer refuses a non-canonical `--custody-ledger` in runtime mode (checked before any H/key work); the out-of-tree canonical is used; `--test` gates fixture overrides — mirroring the driver.
37. **One-time benign attestation-1 re-freeze (§4.2 step 6).** A `benign` classification records a monotone one-per-key `benign-refreeze-used` audit record in the CANONICAL ledger (not a custody state); a SECOND benign classification anywhere is refused (only configuration/retirement remains). `classify-attest1` is one-shot per pending mismatch (repeat/conflicting refused); the subcommand takes both the per-H spend log (`--out`) and the canonical ledger (`--ledger`).
38. **Confirmatory exit-class routing (§4.1).** `run_confirmatory.sh` emits distinct exit codes — 3 = qualification-gate fail (retire → `confirmatory-phase-fail`), 2 = infrastructure fault (resumable halt, NO terminal), 5 = integrity failure (typed halt, setup/receipt mismatch class). The parent driver maps ONLY code 3 to `state:confirmatory-phase-fail`.
39. **Setup-attempt persistence + generations (§4.1a).** Every issued attempt writes its receipt IMMEDIATELY (status `issued` before the call, `completed`/`out_present` after); a no-output attempt is CONSUMED (not reissued beyond the per-call cap); a fresh setup after a full exhaustion runs under a new `setup-gen-N` namespace that resets only the new generation's cap and preserves all prior evidence.
40. **Output-manifest construction failure forfeits (§4.3).** A phase-8a manifest-construction refusal (missing/extra/drifted output) after key-3 generation began persists the per-H `terminated-during-gen-or-attest2-mismatch` terminal AND the canonical custody `forfeited-unspent` before exiting.
41. **Setup-manifest anchoring + exactness (§4.1a).** `verify_setup_manifest` enforces DISCOVERED-SET equality (an unlisted extra is caught) and, given the completion receipt's `setup_manifest_sha256`, compares the manifest BYTES to it (a regenerated manifest is refused); the confirmatory result binds that hash, re-checked at attestation-1.
42. **Full-draw output re-hash (§4.1).** `confirmatory-result.json` records the exact required full-draw output set WITH hashes; restart and attestation-1 re-hash them — a deleted/drifted polarity/retrieval/verify/aggregate/adaptive/composition artifact refuses (not a stale `exists()` boolean).

## Operator runbook — procedural mitigations (REQUIRED)

The workspace enforces custody in code, but a correct real run also depends on these operator procedures (some are the human side of the DEFERRED per-H directory-namespacing limitation). Follow ALL of them:

- **Seed the canonical custody ledger ONCE, before the first run:** `python3 spend.py genesis` (writes the eligible GENESIS to `/mnt/f/hub/10_projects/minelit/idiolect/key3-custody.jsonl`). Every run refuses before phase 0 if this out-of-tree ledger is missing/unreadable/un-seeded. Never delete, edit, or re-seed it; it is the durable cross-run record that a key was spent/forfeited.
- **Single process, NO concurrent invocations.** Run exactly one `run_v010.sh` (and one scorer) at a time. The locks make the claim atomic, but the recovery model assumes a single live instance per H; do not launch a second driver against the same workspace or the same canonical ledger concurrently.
- **One FRESH CHECKOUT per instance.** A revised-prereg / re-freeze / new-H run must start from a brand-new clean checkout (per-H directory namespacing is DEFERRED — receipts/outputs live at fixed paths). Never rebuild H in place: if `runs/H.json` exists the driver self-verifies it and never rebuilds; if a `generation-started` marker for a different H is present the driver HALTs.
- **Attestation-1 mismatch = STOP + classify.** A post-confirmatory attestation-1 mismatch persists a pending-classification terminal and that H never proceeds. Record the operator decision with `python3 spend.py classify-attest1 --resolution benign|configuration --H <H> --out <per-H spend log> --ledger <canonical ledger>`. A `benign` resolution consumes the ONE preregistered re-freeze (a second benign is refused) and requires a fresh checkout + two NEW confirmatory draws; `configuration` retires the configuration. Classification is one-shot.
- **Never point the scorer at a non-canonical ledger in runtime**, and never pass `--test` in a real run (it is for the offline suite only). Runtime uses the fixed canonical manifest (`../peer-reconciliation-test3/freeze-manifest.txt`) and the fixed canonical custody ledger.
- **Do not hand-edit** `runs/`, the custody ledger, setup manifests, phase receipts, or confirmatory results — every one is hash-anchored and a mismatch HALTs the run by design.
