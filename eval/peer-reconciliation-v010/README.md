# peer-reconciliation-v010 — v0.10 generation-hardening run workspace

This is the implementation workspace that a frozen v0.10 pre-registration binds by hash. **Normative spec:** `/mnt/f/hub/10_projects/minelit/idiolect/2026-07-23-v010-generation-hardening-PREREG-DRAFT.md` (rev 9.2, FREEZE-CANDIDATE — workspace state after round-5 fixes: 105 tests, 97 fixtures). Section references (§) point there unless prefixed v08/v09 (the frozen v0.9 instrument: `../peer-reconciliation-test3/prereg-v08.md`, `prereg-v09.md`). Where the spec defers byte-level behavior to "normative artifacts" (§3.6), the artifacts in this workspace ARE those definitions; prose describes intent, the frozen implementation + golden fixtures define behavior, and any divergence resolves in favor of the artifacts.

**Derivation:** everything is derived by COPYING from the frozen v0.9 harness and modifying the copies. No file outside this directory is edited. The frozen v0.9 originals used as the copy source live in `_reference/` (for the gate-fidelity proof). Copied-unchanged files were hash-verified against `../peer-reconciliation-test3/freeze-manifest.txt` (smoke.py, v09.py, the 7 unchanged pipeline prompts, run_calls.sh — all match).

## Blindness constraints (restated — violating these destroys the experiment)

- The sealed answer material (`key/concepts.json`, `key/answer_key.json` of key-3) and the sealed run's per-pair records (`../peer-reconciliation-test3/runs/`) were NEVER opened during this build. Hardening rationale comes only from the spec + `2026-07-20-gate-stress-probes-RESULTS.md` (TRAIN-side diagnosis).
- **Answer-blindness (custody strengthening over v0.9; hardened in round-5).** The generation controller, the verification/composition layer, and both baselines read an **answer-blind `pairs.json`** = `{count, pairs: [{pair_id, term_a, term_b}]}` — the QUESTION (which term pairs to adjudicate), never the relation classes. Two answer-leak channels are closed: (1) `pair_id` is an **OPAQUE token** `sha256(term_a || NUL || term_b)[:16]` (UTF-8, NUL = the 0x00 byte) — NOT the positional `P01–P10` that would encode the class under the harness key schema; (2) the emit order is a **seeded-but-key-independent shuffle** (sorted by the opaque id, a uniform hash), so the sequence carries no `P01–P10` class ordering. (`term_a == term_b` for jingle pairs is inherent — the jingle strings are known/orchestrator-visible and are in the exposure ledger; it is not hidden.) The relation classes (`expected`/`broader_side`) live only in the sealed key and are read by **exactly one component**, `scorer_v010.py`, at the single authorized scoring read (the SPEND, §4.2 step 9). `pairs.json` is produced by `make_pairs_manifest.py` run as an **isolated subprocess** whose only output is the blind `pairs.json` + its hash (custody-logged, phase 0.5); the orchestrator never parses the key in-process. `load_pairs` enforces a strict whitelist schema, rejects symlinks, and rejects any resolved path under a `key/` directory. The scorer joins `pairs.json` ↔ sealed key on the **term pair** (opaque id has no positional meaning).
- No LLM calls were made during the build. All self-tests are offline/deterministic.

## Component map

| File | Role |
|---|---|
| `smoke_v010.py` | Hardened generation controller (copy of frozen `smoke.py`). H1 feedback-guided regen, H2 split regen budget (total 2 / semantic 1), H3+H4 via the base ladder prompt, answer-blind `load_pairs`. All GATE/validator/aggregation/composition functions carried byte-identical from v0.9 (proven by `scripts/verify_gate_fidelity.py`). |
| `v010.py` | Verification + composition layer (copy of frozen `v09.py`). τ machinery unchanged; NO carried stages (every call fresh); `compose` is ANSWER-BLIND — writes composed per-pair verdict records, does not grade. |
| `retrieve_xc_v010.py` | Cross-corpus retrieval (copy of `retrieve_xc.py`); algorithm byte-identical to v0.9; `--v010` + `--no-determinism` (key-3/confirmatory draws are fresh, first execution is the record). |
| `baseline_a.py` | Baseline-A (bare-coinage naive-ask, §3.4): 20 per-term isolated calls, term-as-query top-3 retrieval, parse + one re-ask + key-blind grounding -> records. |
| `baseline_b.py` | Baseline-B (equal-information direct ask, §3.5): 10 A→B isolated calls, `{a_excerpts}`+`{b_corpus}`, parse + one re-ask + key-blind grounding -> records. |
| `serializers.py` | The ONE definition of `{a_excerpts}`, `{b_corpus}`, and the Baseline-A top-3 doc block (§3.6d). |
| `assemble.py` | The ONE definition of assembled baseline prompt bytes (template + serializers), shared by the runners AND the conformance runner. |
| `parser_adjudicator.py` | The ONE total grammar over an adjudicator reply (§3.6a), shared by both baselines. |
| `scorer_v010.py` | **The ONLY key-reading component.** Counterpart-identity adapter + directional mapping + two-direction combination + coverage + §5 decision table + P=1.00 guardrail. Verifies sealed-answer recorded hashes at first read (the SPEND). |
| `attest.py` | manifest-of-manifests **H** (§4.2), the reusable two-point attestation gate, draw receipts carrying H, §4.4 spend-state logging. |
| `probe_explicit_id.sh` | Explicit-ID model probe (§6 step 3): asserts each pinned Claude ID is a member of the CLI's resolved set. **Do not run during build.** |
| `setup_confirmatory.py` | Setup-constructor wrapper (§4.1a): drives the UNCHANGED frozen v0.9 key-author/corpus path twice, attempt accounting (retry cap 1, hash all, none discarded, setup-exhaustion ≠ retirement). |
| `conformance_runner.py` | The §3.6(f) frozen conformance runner over `fixtures/*.json`; MUST PASS; part of both attestation points. |
| `make_pairs_manifest.py` | Answer-blind projection: key structure → `pairs.json` (pair_id/term_a/term_b only). |
| `run_v010.sh` | The key-3 run driver (exact sequence; H1/H2 budget-2 loop). **Not executed during build.** |
| `run_confirmatory.sh` | Runs the tool path on one confirmatory key, counts configFails/40. **Not executed.** |
| `run_calls.sh` | Frozen v0.9 call executor (copied, hash-verified). |
| `prompts/gen-definition-v010.md` | H3 (word-budget emphasis) + H4 (cumulative-extension) base ladder prompt (copy+modify of `gen-definition-v07.md`). |
| `prompts/regen-feedback.md` | H1 feedback regen template (`{FAILING_GATE}`,`{MEASURED_AND_CAP}`,`{CAP_IN_WORDS}`,`{BASE_PROMPT}`). |
| `prompts/baseline-a.md`, `baseline-a-reask.md`, `baseline-b.md`, `baseline-b-reask.md` | Literal baseline templates incl. the full re-ask bytes (§3.6b); Baseline-B's re-ask is its own file. |
| `prompts/{checklist-extract,ladder-conformance,polarity-check,verify-matrix,verify-pair,decompose,containment-v2}.md` | The 7 unchanged v0.9 pipeline prompts (hash-verified). |
| `harness/{keyspec-author.md,validate_key.py,build_briefs.py,gen_leakcheck.py,split_corpus.py}` | Frozen v0.9 key-author/corpus scripts (for the confirmatory setup only). `split_corpus.py` is now IN the workspace (hash e240…, verified vs the frozen record, bound in H); `setup_confirmatory.py` uses it by default. `gen-community-{a,b}.md` are NOT static files — `build_briefs.py` generates them per key (see round-5 note). |
| `fixtures/*.json` | Seed golden fixtures (28) defining the format for the fixture-oracle agent. |
| `toy-key/` | SYNTHETIC toy key + pairs.json + recorded-hashes for the scorer unit tests. No relation to any real key. |
| `tests/test_v010.py` | Offline test suite (61 tests). |
| `scripts/verify_gate_fidelity.py`, `scripts/gen_seed_fixtures.py` | Gate byte-identity proof; seed-fixture generator. |

## Spec-section → implementation mapping

- **§2 H1 feedback-guided regen** → `smoke_v010._route_fail` + `write_regen_prompt` + `prompts/regen-feedback.md` (regen prompt receives failing gate identity, measured value, violated cap; per-generation-index prompt file).
- **§2 H2 / §5 regeneration state machine** → `smoke_v010._route_fail` counters `total_regens` (cap 2) / `semantic_regens` (sub-cap 1); mechanical+leak+generate share the total, semantic capped at 1 within it; max 3 generations (g0,g1,g2); configFail scoped to every pair involving the term. Mixed-class transitions tested.
- **§2 H3 word-budget emphasis + §2 H4 cumulative-extension** → `prompts/gen-definition-v010.md` (base ladder prompt; L2 60–160 enforced-cap emphasis; literal cumulative construction L1=L0+mechanism, L2=L1+metric/conditions). L2 cap stays 60–160 (Q6).
- **§2 gates unchanged** → proven by `scripts/verify_gate_fidelity.py` (60 gate/layer functions byte-identical to frozen v0.9).
- **§5 conformance batching** → `smoke_v010.prompts_conformance`: exactly one ordered batch per side per generation index (keyed by `total_regens`), terms in term order; one malformed re-run per batch; run-halt on still-malformed (`gate_conformance`, unchanged).
- **§3.4 Baseline-A** → `baseline_a.py` (retrieval unit = frozen v0.8 §2.6 verbatim, bare term query; per-term independent; truth table via the parser; both directions; combination in the scorer) + `prompts/baseline-a*.md`.
- **§3.5 Baseline-B** → `baseline_b.py` (`{a_excerpts}` 4–6 in pool order; `{b_corpus}` 11 files filename order; matched_term validation vs corpus) + `prompts/baseline-b*.md`.
- **§3.6(a) parser** → `parser_adjudicator.py`. **(b) templates + re-ask** → `prompts/baseline-*.md`. **(c) fixtures** → `fixtures/` (seed; extended by the fixture agent). **(d) serializers** → `serializers.py`. **(e) isolated single-turn** → each attempt staged as a new call through `run_calls.sh`→`../e2e-cell/run_isolated.sh`. **(f) conformance runner** → `conformance_runner.py`.
- **§4.1a confirmatory setup** → `setup_confirmatory.py`. **§4.1 confirmatory draws** → `run_confirmatory.sh`.
- **§4.2 H + attestation (2 points)** → `attest.py build-H` / `attest`. **§6 probe** → `probe_explicit_id.sh`. **§4.4 spend-state / §4.3 key-3 state** → `attest.py spend-log`.
- **§5 scorer (adapter, coverage, decision table, P guardrail)** → `scorer_v010.py`.

## Exact run sequence (`run_v010.sh`; not executed here)

`projector → freeze → probe → setup → confirmatory draws → attestation(1) → generation → attestation(2) → scoring`

0. offline tests + gate fidelity + conformance runner (no model calls)
0.5. **PROJECTOR** (its source is already committed/immutable; runs BEFORE H so H can bind its output) — `make_pairs_manifest.py key pairs.json --recorded-hashes freeze-manifest.txt --spend-log runs/spend-log.jsonl`: hash-gates the sealed source file, registers a typed `structure:read`, emits blind `pairs.json` + `pairs_sha256:` (custody-logged)
1. FREEZE + `attest.py build-H` → `runs/H.json` (binds the blind `pairs.json` + `recorded_manifest_sha256`)
2. `probe_explicit_id.sh` (2 calls; pinned-ID membership) → `runs/probe-log.json`
3. `setup_confirmatory.py` — 2 fresh TRAIN keys via the frozen v0.9 path (uses the in-workspace `harness/split_corpus.py`)
4. `run_confirmatory.sh` ×2 — tool path per conf key; gate ≤1 failed artifact/40 (a checklist configFail counts 2: the checklist AND its never-created ladder). A fail RETIRES the effective configuration (§4.1)
5. ATTESTATION POINT 1 (pre-generation): STRICT verify-files (H self-checked first; fail on missing/extra runtime or corpora) + clean tree + CLI-version enforced + probe membership + v0.9-carried-artifact hash-verify (incl. isolation wrapper) + live BGE-tree recompute vs H + conformance runner; + receipt
6. key-3 tool-arm generation (answer-blind): excerpts → checklists (H1/H2 loop) → ladders (H1/H2 loop) → conformance (lowest-pending-index batches, coalescing) → polarity → retrieval → verify → aggregate → adaptive-1/2 → compose (composed per-pair verdicts)
7. baselines (answer-blind): A retrieve/prompts/gate(+reask), B prompts/gate(+reask)
8. ATTESTATION POINT 2 (post-generation) + receipt — any mismatch ⇒ invalid pre-scoring, forfeited-unspent
9. **THE SPEND (atomic claim + one bounded relaunch):** driver logs `state:scoring-attempt` (max 2), then `scorer_v010.py` runs the pre-claim gate, H checks, then appends `spend:authorized-read-claimed` UNDER THE LOCK immediately before the first key byte, reads the key → `runs/scores.json`, and appends `spend:authorized-read-complete`. A pre-read failure with attempts<2 → ONE relaunch; a failure AFTER the claim (key spent) or attempts==2 → refused, no relaunch. No post-hoc authorized-read append by the driver.

## Interfaces the fixture-oracle agent needs

- **Parser entry point:** `parser_adjudicator.parse_adjudication(text: str, relation_enum: set) -> {"classification": "positive"|"negative"|"malformed", "reason": str, "fields": {"match","matched_term","relation","evidence"}}`. Arm enums: `RELATION_ENUM_A`, `RELATION_ENUM_B`; helper `enum_for_arm("A"|"B")`.
- **Serializer signatures** (each returns pure content — NO leading/terminal newline; the template owns separators + the single terminal newline; every item incl. the first is labeled):
  - `serializers.serialize_a_excerpts(excerpts: list[str]) -> str` (requires 4≤len≤6; `"N. <text>"` joined by `\n`)
  - `serializers.serialize_b_corpus(docs: list[(label,text)]) -> str` (`"=== DOCUMENT <label> ===\n<rstripped text>"` joined by `\n\n`)
  - `serializers.serialize_baseline_a_docs(ranked_docs: list[(label,text)]) -> str` (`"--- RANK <n> (document <label>) ---\n<text>"` joined by `\n\n`)
  - `serializers.concat_docs_text(docs) -> str` (general concatenation utility; NO longer the grounding path — grounding is per-single-document, see BUG-1 fix)
- **Assembly signatures:** `assemble.assemble_baseline_a(term, ranked_docs, reask=False) -> str`; `assemble.assemble_baseline_b(term_a, excerpts, docs, reask=False) -> str` (single-pass substitution — data containing placeholder tokens is inserted verbatim).
- **Template placeholder names:** Baseline-A `{TERM}` `{RETRIEVED_DOCS}`; Baseline-B `{TERM_A}` `{A_EXCERPTS}` `{B_CORPUS}`; ladder `{CHECKLIST}`; regen `{FAILING_GATE}` `{MEASURED_AND_CAP}` `{CAP_IN_WORDS}` `{BASE_PROMPT}`.
- **Fixture format + runner:** each fixture is a JSON object `{"kind", "name", "why", ...inputs, ...expected}`; kinds `parse | serialize_a_excerpts | serialize_b_corpus | serialize_baseline_a_docs | assemble_baseline_a | assemble_baseline_b | ground | pairs_manifest` (schemas in `conformance_runner.py`). `pairs_manifest` pins the §3.6 answer-blind pairs.json schema: `{"term_pairs": [[a,b],...], "expect": {count, pairs}}` — `build_payload` must yield top-level `{count, pairs}`, per-record exactly `{pair_id (16-hex), term_a, term_b}`, `pair_id = sha256(term_a‖NUL‖term_b)[:16]`, sorted by `pair_id`. `python3 conformance_runner.py` runs all `fixtures/*.json` and MUST exit 0. Seed fixtures are labeled `"oracle": "independent"` (hand-specified) or `"regression-seed"` (impl-derived bytes — the fixture agent should replace these with independent byte oracles for full §3.6(c) coverage).
- **Scorer entry:** `scorer_v010.py --key-dir <dir> --recorded-hashes <file> --pairs <pairs.json> --H <runs/H.json> --spend-log <log> [--tool-verdicts ...] [--baseline-a ...] [--baseline-b ...] --out <scores.json>`. The key path is a parameter and never resolves during build/test. The scorer checks the locked spend gate, verifies H self-consistency, binds `--recorded-hashes` to H, and verifies sealed recorded hashes before the SPEND.
- **Grounding helper (arm-specific, key-blind):** `baseline_a._ground(fields, docs)` / `baseline_b._ground(fields, docs)` — each field grounded iff it is a substring of at least one SINGLE document (BUG-1 fix); `docs` = list of `(label, text)`.

## Deviations from the spec / judgment calls (feed round-5)

1. **Answer-blind `pairs.json` split (the load-bearing custody decision) — hardened in round-5.** The spec requires the scorer to be the only component referencing the sealed key path, and step-7 generation to be "sealed-answer-material-blind," yet retrieval + verify + adaptive stages structurally need the *pairing*. Resolution: the pairing is the QUESTION (answer-blind), supplied via `pairs.json` with **opaque `sha256(terms)` ids in a key-independent shuffle** (no `P01–P10` positional class encoding) and a **strict-whitelist, symlink-safe** `load_pairs`; the ANSWER (`expected`/`broader_side`) is read only by the scorer, which joins on the term pair. The projection runs as an **isolated subprocess** (phase 0.5) emitting only the blind `pairs.json` + hash, custody-logged; the orchestrator never parses the key in-process. **Residual flagged for round-5:** for key-3 the isolated projector still *parses* the sealed `concepts.json` (which contains classes/descriptions) to extract the terms+pairing, discarding the answer — a bounded, logged read, not the SPEND. If round-5 judges even the isolated parse unacceptable, the blind `pairs.json` must be emitted by the isolated key-author at seal time instead.
2. **Parser wrapper-quote stripping (refined in round-5).** The parser strips at most one **genuinely matched** pair of surrounding double quotes — ASCII `"…"` OR curly `“…”`; **mixed** wrappers (`"…”`, `“…"`) are NOT stripped. Pinned by 4 fixtures (2 matched-strip, 2 mixed-not-stripped). A stricter "no stripping at all" variant remains possible; flagged.
3. **Conformance batch key = generation index** (not v0.9's incrementing wave counter). Implements §5's "one ordered batch per side per artifact-generation index" literally; equivalent to v0.9's waves under the driver's sequencing, but keyed to the spec's unit.
4. **`split_corpus.py` is now IN the workspace** (round-5): copied from the frozen v0.9 tree, hash-verified against the freeze-manifest recorded value (e240…), bound in H, and used by `setup_confirmatory.py` by default (the earlier operator-supplied `--split-script` deviation is dropped). **Correction to the round-5 review re `gen-community-{a,b}.md`:** these are NOT generic static files and were NOT added. `build_briefs.py` (frozen, in H) generates them PER KEY from that key's `concepts.json`; the confirmatory path generates fresh ones per conf key, and the key-3 v0.10 path does not regenerate corpora (they are frozen precondition inputs), so no static `gen-community` file is referenced. The key-3 `gen-community-{a,b}.md` recorded as 967…/22e9… in the freeze-manifest are the SEALED, key-3-specific briefs (they embed key-3's concept descriptions per `build_briefs.py`), so copying them would breach blindness — they are deliberately not in the workspace.
5. **`retrieve_xc_v010.py --no-determinism`.** Key-3 and each confirmatory draw are fresh (no prior v0.8 retrieval baseline), matching the frozen key-3 TEST harness which added the same flag; the retrieval algorithm is byte-identical to v0.9.
6. **Confirmatory draws run in a copied workspace** (`run_confirmatory.sh` copies the instrument into each conf-key dir), matching the frozen precedent (test3 = the v0.9 package copied verbatim per key), so no in-place workspace parameterization was added (that would have broken the gate byte-identity proof).

## What is tested offline vs untested

**Tested (deterministic, no model/venv):** extended gate byte-identity (63 carried functions + 11 module globals + imports, reference hash-bound to the frozen record); the H1/H2 state machine (split budget, semantic sub-cap, both mixed-class orders, max-3, leak shares budget, generate-malformed non-semantic, H1 feedback-prompt content); conformance batching — only the lowest-pending index staged, and a late g0→g1 entrant COALESCES with a mechanical g1 entrant (no stranding; `assert_resolved` passes); the full parser grammar (every branch) incl. matched-vs-mixed wrapper quotes; serializers at 4/5/6 + first-doc labeling + terminal-newline + rstrip; single-document grounding + single-pass substitution; the scorer (adapter, mapping, all combination rows, coverage, decision table, sealed-hash custody, H self-check tamper, opaque term-pair join); the **atomic-claim spend** (claim one-shot; scoring-attempt capped at 2; one pre-read failure then relaunch; crash-after-claim refuses a second invocation; zero structure-reads refused; untyped/answer-read refused; accidental blocks all); opaque-id shuffle blindness; strict-whitelist + symlink + under-`key/` + slug-collision rejection; **strengthened attestation** (STRICT verify-files fails on missing/extra runtime + missing corpus; recorded-artifact hash-verify incl. isolation wrapper vs the frozen record; live BGE-tree recompute + mismatch detection); `attest build-H` determinism + `recorded_manifest_sha256` binding; the fidelity proof catching a mutated global; the projector hash gate (abort with nothing read + no `structure:read` on mismatch); the `pairs_manifest` §3.6 schema. **Per-kind fixtures (97):** parse 57, ground 15, serialize_a_excerpts 5, serialize_b_corpus 5, serialize_baseline_a_docs 4, assemble_baseline_a 3, assemble_baseline_b 5, pairs_manifest 3. **105 tests + 97 conformance fixtures + 2 verifier scripts, all green.**

**Untested offline (require LLM CLIs / the BGE venv / real corpora / the sealed key — exercised only at run time):** any actual model call; retrieval execution (BGE embeddings) for the tool arm and Baseline-A; the end-to-end generation loop against real corpora; `probe_explicit_id.sh`; `setup_confirmatory.py` non-`--dry-run`; `run_confirmatory.sh`'s copied-workspace tool-path run; the `attest attest` clean-tree / CLI-version-enforce / probe-membership checks; the scorer's read of the *real* sealed key (only the toy key is exercised).

## Spec-wording amendments implied for the freeze fold (running list)

These are artifact-vs-prose divergences the artifacts resolve; the frozen spec text should be amended to match at the freeze fold (artifacts are normative, §3.6):

1. **Single-document grounding** — §3.4/§3.5's "a contiguous substring of the concatenated retrieved documents" → "a contiguous substring of at least one individual retrieved document" (a boundary-spanning span validates in no single document and must fail; per the BUG-1 fix).
2. **Checklist-exhaustion counting** — §4.1's confirmatory numerator counts a checklist configFail as **2** failed artifacts (the checklist AND its never-created ladder) against the fixed denominator 40.
3. **Projector custody step** — the answer-blind `pairs.json` is produced by an isolated-subprocess projector (phase 0.5), custody-logged and bound in H, before generation; `pair_id` is an opaque hashed token in key-independent shuffled order (no `P01–P10` positional class encoding). The projector (a) HASH-GATES the sealed source file against the recorded value in the committed `freeze-manifest.txt` before parsing (abort with nothing read on mismatch = `state:abort-before-gen`), and (b) registers a single typed `structure:read` (NON-SPEND) in the ONE authoritative locked spend log the scorer + attest read. The pairs.json schema is itself a §3.6 committed artifact, fixture-backed (`pairs_manifest` kind). The spec's custody wording (§4.3/§4.4) should name the `structure:read` typed entry as a sanctioned non-spend reader alongside the scorer's authorized read.
4. **Opaque-id formula** — the blind-manifest `pair_id` is `sha256(term_a || NUL || term_b)[:16]` (UTF-8, NUL byte 0x00); §3.6(d′) and this README's top section both use the NUL form (the delimiter-free `term_a || term_b` wording in the spec should be corrected to match).
5. **Projector-before-H ordering** — §4.2 must renumber Phase 0.5 BEFORE the H-building step and state that the projector *source* is already committed/immutable (so it runs first, then H binds both that source and its `pairs.json` output). The workspace already runs `projector → build-H`.
6. **Parser field order** — §3.6(a) says "exact field set and order," but the fixtures deliberately pin ORDER-AGNOSTIC parsing (`parse_field_order_reversed_*` expect success); §3.6(a) should read "exact field set, order-agnostic" (the prompt still states a preferred order). No code change — the artifact already parses any order.
7. **Atomic-claim spend transition** — §4.4 must describe scoring as: driver `state:scoring-attempt` (max 2) → scorer `spend:authorized-read-claimed` under the lock immediately before the first key byte → `spend:authorized-read-complete`; a pre-read failure with attempts<2 permits ONE relaunch, a claim (or attempts==2) forbids any further read.
