You are given research notes describing a concept, followed by 18 records (titles and abstracts) in arbitrary order. Identify the 5 records most likely to be the established research literature on the same underlying question the notes are asking. Return ONLY a ranked list of the 5 record IDs, most likely first, one per line, nothing else.

[Frozen permutation rule: records are appended below in ascending order of sha256(title) — deterministic, no RNG, fixed before any run.]

NOTES:
