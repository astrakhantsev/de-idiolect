Below are usage excerpts from one community's documents. The term under study is masked as ⟦TERM⟧; other local jargon is masked as ⟦X⟧.

Extract a checklist of 4–7 concrete commitments that ANY faithful definition of ⟦TERM⟧'s concept must state, based ONLY on these excerpts:
- the SPECIFIC mechanism or process involved (what concretely happens — this item is mandatory),
- what is measured or produced, and how it is scored or read,
- when/where it applies (the setting and trigger),
- any constraint the excerpts clearly commit to.

Rules: each item is one line, concrete, supported by the excerpts; do NOT generalize beyond what the excerpts support; do not include ⟦X⟧ concepts. Output ONLY the checklist lines, one per line, no preamble.

EXCERPTS:

1. The scripted follow-up either redirected the target component or withdrew the requested change entirely. ⟦TERM⟧ was measured over 360 interrupted trajectories. ⟦TERM⟧ reached 0.79 for the instruction-tuned system and 0.54 for the base system, with stale diffs accounting for most unsuccessful switches.
2. ⟦TERM⟧ was measured over 360 interrupted trajectories. ⟦TERM⟧ reached 0.79 for the instruction-tuned system and 0.54 for the base system, with stale diffs accounting for most unsuccessful switches. An objective checker was run after every executable action and marked the earliest point at which the revised task was fully satisfied.
3. This separation avoided attributing queueing delays to slow reconsideration. ⟦TERM⟧ was analyzed after stratifying trials by the number of actions already taken at interruption. ⟦TERM⟧ declined from 0.82 in early interruptions to 0.61 after an initial patch had been written.
4. ⟦TERM⟧ was analyzed after stratifying trials by the number of actions already taken at interruption. ⟦TERM⟧ declined from 0.82 in early interruptions to 0.61 after an initial patch had been written. Agents that failed commonly retained a correct implementation of the original request and appended an incomplete revision.
5. ⟦TERM⟧ was 0.73 after adjustment for task length. ⟦TERM⟧ was 0.16 lower when the reversal arrived in a non-English instruction than in the English control.
6. ⟦TERM⟧ was 0.73 after adjustment for task length. ⟦TERM⟧ was 0.16 lower when the reversal arrived in a non-English instruction than in the English control.
7. ⟦TERM⟧ was collected from a smaller interrupted subset. ⟦TERM⟧ showed no reliable selector effect, although the interval was wide.
8. ⟦TERM⟧ was collected from a smaller interrupted subset. ⟦TERM⟧ showed no reliable selector effect, although the interval was wide.