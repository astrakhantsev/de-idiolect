You are a research librarian with web access. Answer from your knowledge AND from web search. Tools: WebSearch, and the shell command `safefetch <url>` (called bare, exactly like that) for fetching page content. Never use WebFetch. Do all of this work yourself, inline, in this session — do NOT delegate to subagents, background tasks, or any research workflow; delegated work is lost. Open at least 3 sources in full and quote from their bodies (not abstracts), attributing each quote. If a fetch fails or is blocked, say so and move on; never invent a quote. If web search is unavailable, answer from knowledge and mark every citation as unverified-recall.

A proposed system, stated operationally: (1) a detector monitors a project's or community's working text and flags when a term is being coined — including when a common word is being used in a project-specific sense; (2) at that moment, a language model automatically writes a detailed operational definition of the flagged term in a constrained common vocabulary (no project jargon, no proper names, no field names) — lazily and locally: per project, on demand, with no global shared ontology, lexicon, or terminology standard ever constructed or maintained; (3) the definition, not the term, becomes the search key: it is stored alongside an embedding and used to retrieve other communities' concepts whose definitions match, so that the same concept under different names in different fields can be found; candidate matches are then verified pairwise and assigned a typed relation (equivalent / broader / narrower / related).

Questions:

1. Who has proposed or built this exact assembly, or the closest published versions of it? Systems from 2023-2026 that use LLM-generated definitions or glosses for retrieval, terminology work, ontology construction/alignment, or novelty checking all count. Name the closest 3-5 with citations, and state precisely what each one does and does not cover of components (1)-(3).
2. For each component taken separately — (a) detection of newly coined terms and of common words used in a novel project-specific sense; (b) automatic generation of definitions for terms; (c) using definitions/glosses as the representation for matching or retrieval; (d) writing definitions in a constrained/controlled defining vocabulary — what is the established field term for that problem, and who is the oldest owner you can find? At least one pre-2015 source per component.
3. Is the specific architectural stance "generated lazily on demand, per community, with no global resource ever materialized" distinguishable in the published literature, or has that stance itself been articulated somewhere?
4. In one sentence, in the field's own vocabulary: what would an expert in terminology science or ontology matching say this proposal IS?

Output format: for each system/component, a block with: name · owner + citation · 1-2 verbatim body quotes with source attribution · your confidence · what you could not verify. End with two lists: the oldest relevant sources you found, and what you did NOT search (honest coverage gaps).
